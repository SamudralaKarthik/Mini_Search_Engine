next prev Screen Rotation Using Python Tkinter this module, will create Python code rotating screen into with GUI. Using some functions from rotatescreen module, simple Python library rotating screen system, display changed four orientations. Installing rotate-screen install rotate-screen Walking through code Step Importing packages from Python library order them code. imported Tkinter rotatescreen. Importing reqd. packages from tkinter import import rotatescreen Step Creating function Scr_Rotation() used calling where screen needs rotated. Also, created variable called store detect default screen orientation. Defining function order used rotate screen Scr_rotation(flag): Getting default display screen rotatescr.get_primary_display() Step Under Scr_Rotation() function, will introduce different methods change orientation screen. rotate screen upwards, will set_Landscape() method. rotate screen rightwards, will set_portrait_flipped() method. rotate screen downwards, will set_Landscape_flipped() method. rotate screen leftwards, will set_portarait() method. Rotating screen upwards attain landscape view flag "up" scr.set_landscape() Rotating screen rightwards attain flipped portrait view elif flag "right" scr.set_portrait_flipped() Rotating screen downwards attain flipped landscape view elif flag "down" scr.set_landscape_flipped() Rotating screen leftwards attain portrait view elif flag "left" scr.set_portrait() Step Finally, will create buttons each these orientations. Clicking these buttons, orientation will changed. Creating buttons order change view Button(base, text "Up" command lambda: Scr_rotation( "up"), "white").grid(row column Button(base text "Right" command lambda: Scr_rotation( "right"), "white").grid(row column Button(base text "Left" command lambda: Scr_rotation( "left") "white").grid(row column Button(base text "Down" command lambda: Scr_rotation( "down") "white").grid(row column rest general widgets explained along code itself program. Complete Code Python program Build Screen Rotation Using Tkinter Python Importing reqd. packages from tkinter import import rotatescreen Defining function order used rotate screen Scr_rotation(flag): Getting default display screen rotatescr.get_primary_display() Rotating screen upwards attain landscape view flag "up" scr.set_landscape() Rotating screen rightwards attain flipped portrait view elif flag "right" scr.set_portrait_flipped() Rotating screen downwards attain flipped landscape view elif flag "down" scr.set_landscape_flipped() Rotating screen leftwards attain portrait view elif flag "left" scr.set_portrait() Creating instance tkinter object base Tk() Setting dimensions screen base.geometry("100x100") Giving title screen base.title("Screen Rotation") Setting color base.configure(bg #54c5d1) classes tkinter final StringVar() Creating buttons order change view Button(base, text "Up" command lambda: Scr_rotation( "up"), "white").grid(row column Button(base text "Right" command lambda: Scr_rotation( "right"), "white").grid(row column Button(base text "Left" command lambda: Scr_rotation( "left") "white").grid(row column Button(base text "Down" command lambda: Scr_rotation( "down") "white").grid(row column Calling main loop execute program mainloop() Output: screenshot output code jotted above given below. orientation changed clicking these buttons, whichever user wants. Next TopicApplication Search Installed Applications using Tkinter Python prev next