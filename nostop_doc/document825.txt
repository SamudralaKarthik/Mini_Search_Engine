next prev Matplotlib.figure.Figure.add_subplot() Python What Matplotlib? Python, have inbuilt libraries which have useful inbuilt functions which importing these libraries. Matplotlib most important libraries Python, which used draw plots figures various types using inbuilt functions. Matplotlib, have figure module which used draw plots well control layouts plots. add_subplot() function add_subplot() function, which used axes subplots figure. helps determining layout figure. Syntax: add__subplot(self,*args,**kwargs) have different ways function call this function: add_subplot(nrows,ncols,index,**kwargs) add_subplot(position,**kwargs) add_subplot(ax) add_subplot() Parameters: *args: This parameter means that there would some number arguments like number rows, number columns index value. index value will start from right value column. define index value with integers also, which will define column values. example: add_subplot(5,4,3): means that there would five rows four columns, subplot would third column. add_subplot(5,4,(2,2)): means that there will five rows four columns, subplot will from column first till row. projection: optional parameter which accepts values like 'hammer','polar','lambert' etc., which defines projection type subplot. polar: accepts boolean value, true, then projection would type 'polar'. sharex,sharey: shares x-axis y-axis with sharex sharey. label: accepts string values, which sets labels axes subplot. Return type: This function returns axes subplot, which plotted figure. Example1: Implementation Matplotlib module's add_subplot() function import Matplotlib.pyplot from mpl_toolkits.axisartist.axislines import Subplot plt.figure(figsize =(5, Subplot(fig, 111) fig.add_subplot(axs) axs.axis["right"].set_visible(False) axs.axis["top"].set_visible(False) fig.suptitle('Matplotlib.figure.Figure.add_subplot()function example from javatpoint', fontweight ="bold") plt.show() Output: Explanation: above code, have imported necessary modules, like Matplotlib etc., their functions. Now, subplot function, have value 111, which means will have column subplot will index1. used add_subplot function plot this subplot, subplot whole area. have visibility right axis false only left bottom axis plot. Example2: Implementation Matplotlib module's add_subplot() function import Matplotlib.pyplot from mpl_toolkits.axisartist.axislines import Subplot plt.figure(figsize =(6, Subplot(fig, 222) fig.add_subplot(axs) axs.axis["right"].set_visible(False) axs.axis["top"].set_visible(False) fig.suptitle('Matplotlib.figure.Figure.add_subplot()function example from javatpoint', fontweight ="bold") plt.show() Output: Explanation: then complete area would divided into rows columns then subplot will index two, which will first second column. Example Implementation Matplotlib module's add_subplot() function import Matplotlib.pyplot import numpy np.random.seed(16984251) x_data np.random.random([5, 10]) x_data1 x_data[0, x_data2 x_data[1, y_data1 x_data1 y_data2 x_data2 plt.figure() axs= fig.add_subplot(2, axs.plot(x_data1, y_data1, color ='tab:red') axs.plot(x_data2, y_data2, color ='tab:orange') axs.set_xlim([0, axs.set_ylim([0, fig.suptitle('Matplotlib.figure.Figure.add_subplot() exmples from Javatpoint', fontweight ="bold") plt.show() Output: Explanation: above code, used random dataset from numpy, took datasets x-axis y-axis each, respectively. Then plotted them using add_subplot function where have defined values 2,1,1, which means whole figure divided into rows column. Then subplot first column will present upper half figure. Next TopicPython functions int(bit_length,to_bytes from_bytes) prev next