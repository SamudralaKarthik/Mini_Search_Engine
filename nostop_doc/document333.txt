next prev String Python this tutorial, will learn ways converting string integer Python- have look example before proceeding- a='Learning Python fun' #Displaying type print(type(a)) print(type(b)) Output: <class 'str'> <class 'int'> above example, have declared variables with string integer value respectively. verify their data types using type(). question that arises here need convert string integer. following program illustrates same- value_a "100" value_b "26" value_a value_b print("The multiplication val_a val_b gives: ",res) Output: value_a value_b TypeError: can't multiply sequence non-int type 'str' Since generates this type error, this reason that must convert string values integers that easily proceed with operations. It's time have look first program that demonstrates converting string integer. print(type(a)) #using int() conv_a=int(a) print(type(conv_a)) conv_a conv_a+10 print(conv_a) print(type(conv_a)) Output: <class 'str'> <class 'int'> <class 'int'> Explanation: Let's explanation above program- first step declare variable with string value. After this, have checked data type using type(). converting string integer, have used int() then checked type. have operated variable adding Finally, resultant value displayed output. Approach-2 next example, will indirect approach converting string integer. following program shows done- value_a "100" value_b "26" print(type(value_a)) print(type(value_b)) #converting float value_a=float(value_a) #converting value_b=int(value_b) res_sum=value_a+value_b print("The value_a value_b ",res_sum) Output: <class 'str'> <class 'str'> value_a value_b 126.0 Explanation: understand what have done above program- first step declare variables 'value_a' 'value_b' with string value. After this, have checked their data type using type(). converting string integer, have used float() convert string float value. next step, will convert string value 'value_b' integer. have added 'value_a' 'value_b' printed their sum. Finally, resultant value displayed output. Approach-3: last program, will discuss more scenario converting string Python. Here will convert number present string value base when different bases. following program illustrates same- num_value '234' printing value num_value print('The value num_value num_value) #converting base assuming base print('The value num_value from base base is:', int(num_value)) #converting base assuming base print('The value num_value from base base int(num_value, base=8)) #converting base assuming base print('The value num_value base base int(num_value, base=6)) Output: value num_value value num_value from base base value num_value from base base 156 value num_value base base Explanation: It's time have glance explanation above program. first step, have declared value variable. Since output will always base have provided different base values inside int(). base values have taken here executing program, expected output displayed. Conclusion this tutorial, learned different ways converting string value. Next TopicInternet Things with Python prev next