next prev Random Uniform Python this tutorial, will discuss uniform() method present random module Python also used importing NumPy. uniform() returns floating-point number range numbers between specified limits. syntax using uniform() random.uniform(a,b) Here, represents upper bound represents lower bound. have look some examples that demonstrates usage Python programs. following program illustrates used python program- Import random module import random Initialize upper lower limits Displaying random number print ("The random number between #using random.uniform() print(random.uniform(x,y)) Output: random number between 10.14646142251812 Explanation- Let's understand what happened above program- Since have method uniform(), have imported random module. After this, have initialized upper bound lower bound which Finally, have passed these values parameters uniform(). executing this program, expected output displayed. have glance more program where have followed same approach provided float values upper lower bounds. Import random module import random Initialize upper lower limits 14.3 Displaying random number print("The random number between 14.3 #using random.uniform() print(random.uniform(x,y)) Output: random number between 14.3 11.521121715281813 Explanation- procedure same previous program here observe displays required output even when provide decimal values. know that NumPy module used Python carry different mathematical operations because kind in-built functions this module provides, code becomes less complex more efficient. uniform() here, Consider program given below, Importing NumPy module import numpy np.random.seed(55) Creating array size four num_arr np.random.uniform(size high Displaying values array print("The resultant array num_arr) Output: resultant array [0.09310829 0.97165592 0.48385998 0.2425227 Explanation- It's time know explanation above program- Since have method uniform(), this time have imported NumPy module. next step provide value random.seed() since used initialize random number generator. After this, have initialized values size array, upper bound lower bound which 4,0 respectively inside np.random.uniform(). have declared num_arr using np.random.uniform() because generating array here. executing this program, expected output displayed which array consisting three values. Now, let's check another program- Importing numpy module import numpy np.random.seed(0) Creating array size four num_arr np.random.uniform(size high #Displaying values array print("The resultant array num_arr) Displaying type num_arr print(type(num_arr)) Output: resultant array [[0.5488135 0.71518937 0.60276338] [0.54488318 0.4236548 0.64589411] [0.43758721 0.891773 0.96366276]] <class 'numpy.ndarray'> Explanation- Let's understand what happened here, Since have method uniform(), this time have imported NumPy module. next step provide value random.seed() since used initialize random number generator. After this, have initialized values size array(this time have created two-dimensional array), upper bound lower bound which (3,3), respectively inside np.random.uniform(). have declared num_arr using np.random.uniform() because generating array here. executing this program, expected output displayed which array consisting three values also type num_arr. Finally, it's time discuss last program this article, #Importing numpy module import numpy np.random.seed(0) #Creating array size five num_arr=np.random.uniform(size high 63) #Displaying values array print("The resultant array num_arr) #Displaying type num_arr print(type(num_arr)) Output: resultant array [53.52508358 57.01897669 54.6580309 53.44254684 50.89675079] <class 'numpy.ndarray'> Explanation- Since have method uniform(), this time have imported NumPy module. next step provide value random.seed() since used initialize random number generator. After this, have initialized values size array, upper bound, lower bound which 63(this time have taken definite range) respectively inside np.random.uniform(). have declared num_arr using np.random.uniform() because generating array here. executing this program, expected output displayed which array consisting three values also type num_arr. Conclusion this tutorial, learned what uniform() used various python programs. Next TopicRelational Operators Python prev next