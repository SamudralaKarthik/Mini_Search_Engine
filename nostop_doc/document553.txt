next prev Convert Roman Number Decimal (Integer) Write Python Program Convert Roman Integer this tutorial, will write Python program convert Roman numbers into integer. popular problem asked tech giant Amazon, Facebook interview. Let's problem statement implementation solution. Problem Statement roman number given string; task convert corresponding integer value. symbols given below reference. Symbols Values IX XL XC 1000 Example Input:s VI Output: Example Input: XOutput: 10XL Roman symbol which represents Solution Approach Algorithm First, split Roman numeral string into roman symbols. separate symbol convert each symbol Roman Numerals into value represents. Picking value starting from index current value symbol greater than equal next symbol, then this value returning total. Else subtract this value adding value next symbol running total. Let's implement algorithm Python program. Program rom_value(r): 'I'): return 'V'): return 'X'): return 'L'): return 'C'): return 'D'): return 'M'): return 1000 return romanToDecimal(str): while len(str)): rom_value(str[i]) len(str)): rom_value(str[i Comparing both rom_values n2): str1 else: rom_value current symbol greater equal next symbol str2 str1 else: str1 return print(romanToDecimal("VII")) Output: Explanation above code, define rom_value() function which returns corresponding symbol. Next define romanTointeger() method which converts value roman value integer. romanToInteger() method, have defined variable with while iterated till smaller than length string. converted first character into integer stored into n1. Then, condition check i+1th element smaller than length string. returns true, converts into integer stored n2. Compare n2; greater than increase value one. returns false, then subtract make increment first condition returns false into make increment Complexity Analysis Time Complexity: O(n), where length string. Only traversal string required. Space Complexity: O(1). extra space required Next TopicCreate REST using Django REST Framework Django REST Framework Tutorial prev next