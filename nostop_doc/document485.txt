next prev change size figure drawn with matplotlib? this tutorial, will discuss adjust size figures created with matplotlib Python. Matplotlib library widely used creating graphs. Introduction Matplotlib popular creating figures representing data. populate figure with different types data, including axes, graph plot, geometric shape, etc. perform various operations matplotlib generated graph where adjusting sizing common task. Sometimes, require setting size figure specific size. graph wider size, taller height, etc. matplotlib. figure() provides figsize attribute that specifies width height figure in-unit inches. Let's understand following syntax. Syntax: import matplotlib.pyplot figure_name plt.figure(figsize=(width, height)) figsize optional parameter figure() method. default, figure dimensions (6.4, 4.8). Generally, each unit inch pixels. However, change pixel unit inch using dpi parameter, which specified same function. will follow below approach. will define variable val_1, equal plt.figure(figsize=(6,3)). will create figure object with width inches inches height. figsize attribute value tuple values. Let's understand following example. Example start importing matplotlib import matplotlib.pyplot Plotting figure width height plot_1 plt.figure(figsize=(6, Let's plot equation y=2*x 6,7] [2,4,6,8,10, [x*2 plt.plot() specifies arguments x-axis y-axis plotted plt.plot(x, show this figure object, line, fig.show() plt.show() Output: Example importing matplotlib library import matplotlib.pyplot plot figure width height plt_1 plt.figure(figsize=(2, Using equation y=3*x [3,8,27,64,125] [x*2 plt.plot() determines arguments x-axis y-axis plotted plt.plot(x, show this figure object, line, fig.show() plt.show() Output: above code, have increased height compare width changed graph plot. also size subplots using same method. Let's understand following example. Example import matplotlib.pyplot fig, axes= plt.subplots(nrows=2, ncols=1,figsize=(8,4)) x= [1,2,3,4,5] y=[x**3 axes[0].plot(x,y) axes[1].plot(x,y) plt.tight_layout() plt.show() Output: Height Width Figure Matplotlib also figure's height width without using figsize argument. done using set() function with figheight figwidth arguments. set_figheight() set_figwidth() functions also used height width. Let's understand following example. Example import matplotlib.pyplot import numpy np.arange(0, 0.2) np.sin(x) np.cos(x) plt.figure() fig.set_figheight(4) fig.set_figwidth(8) Adds subplot position fig.add_subplot(121) Adds subplot position fig.add_subplot(122) ax.plot(x, ax2.plot(x, plt.show() Output: other hand, also set_size_inches(). Below example. Example import matplotlib.pyplot import numpy plt.figure() fig.set_size_inches(10, Adds subplot position fig.add_subplot(121) Adds subplot position fig.add_subplot(122) ax.plot(x, ax2.plot(x, plt.show() Output: Modifying Graph another modify graph attributing without using figure environment. also update matplotlib.rcParams, instance RcParams handling default matplotlib values. Let's understand following example. Example import matplotlib.pyplot plt.rcParams['figure.figsize'] range(2, plt.plot(x, plt.show() Output: Conclusion have discussed several ways change size figure matplotlib. These figures very useful data visualization. option that suit your requirement. Next TopicKeyboard Module Python prev next