next prev Python Substring String definition string multiple code character/s series. string includes number collection characters that include alphanumeric special characters, respectively." Python language, strings most common styles. literally insulating characters quotations, build them. Single quotes handled Python similar double quotes. It's easy build strings allocate value variable. Example- Variable1 "Hello Python" Variable2 "Welcome world Python" Pre-defined String Methods Here, going discuss some methods that used manipulate strings python. They tabulated below- Method Description Center() space-padded string with input string positioned complete width columns returned. Count() beginning list start stopping index stop specified, used count number times 'str' occurs sequence substring string. Capitalize() This method used capitalize first letter string. endswith() Defines string substring string finishes with suffix beginning list stopping index offered); returns true wrong not. encode() This method returns encrypted string representation string; fault appears, ValueError raised default until errors specified 'ignore' 'replace.' decode() This technique will allow codec authorized encryption decrypt string. Advanced encoding standard string encoding. expandtabs() used extend string sections multiple spaces; sections given default, spaces section needed. find() This method determines whether takes place string substring beginning ending index returned index figured otherwise. index() index() method works similar like find() method identified, therefore, poses exception. isalpha() sequence least letter letters alphabet false, returns true. isalnum() This method returns true sequence includes least single letter letters alphanumeric false. islower() This method used return true string contains only single letter cased letters lies lower case; otherwise, returns false. isdigit() used return true sequence digits otherwise, false. istitle() Istitle() method returns true sequence appropriately 'titlecase' otherwise returns false. isnumeric() used return valid there only numeric letters unicode sequence, otherwise, returns false. isspace() string only whitespace letters, this method returns true, otherwise returns false. isupper() string contain least single cased letter cased letters capital form, isupper() method becomes true, otherwise becomes false. len() len() method helps find size length string. join() This function consolidates (concatenates) item sequence depictions series into string with string separator. maketrans() This method will allow return conversion chart convert operation used. lower() This will help transform string's upper-case characters into lowercase. ljust() This will used allocate space-padded sequence maximum column widths with initial string left-justified. istrip() This method will help eliminate trailing string whitespace. replace() used displace existing manifestations string with manifestations maximum given, maximum manifestations most. max() This method used return maximum alphabetical letters from sequence str. min() This method used return minimum alphabetical letters from sequence str. rjust() will help return space-padded sequence maximum column widths with initial string right-justified. rfind() working rfind() method similar like find() method searches backward string. rindex() working rindex() function similar like index() function look backward string. strip() This method used perform functionality rstrip() lstrip() function. startswith() used decide whether sequence substring sequence begins with beginning index finishing index granted); returns true false not. split() used split sequence substring (storage supplied) returns substring array; supplied, separated into maximum number substrings. spilitlines() will used separate string NEWLINEs num) returns list each with deleted NEWLINEs. translate() used convert strings (256 chars) conversion table, eliminating those series. swapcase() swapcase() method used Reverse condition sequence letters. title() used Return string's description edition, i.e., terms start upper case remainder lower case. isdecimal() used return true there only decimal letters unicode sequence false, instead. zfill() This method will help return original left-padded sequence zeros maximum letters width; zfill() preserves specified signature digits (less zero). upper() This will help transform string's lower-case characters into uppercase. rstrip() used Eliminate preceding sequence whitespace. What Substring substring sequential character segment within string proper language analysis computer science." other words, portion string substring. Python string specifies several techniques constructing substring, checking includes substring, substring index, etc." example, substring "the better of" better times". This mistaken with substring, which substring sweeping generalization. instance, "Itwastimes" better times" series, substring. this document, will explain python substring functions simplest terms. Creation Substring construct substring with help string slicing. should split() method accomplish array substrings based mentioned delimiter. Syntax syntax string creation python given below- 'Welcome python world' name s[10:] substring creation with help slice print[name] s.split() Print(A1)			// Array substring with help split() method Here, index starts from Example 'Welcome python world' s[10:] 'python' s.split() ['Welcome', 'to', 'the', 'python', 'world'] Output: After successful execution code, below-mentioned screen. Checking found substring method find() operator evaluate whether substring available sequence not. Syntax syntax checking substring that will string given below- 'Welcome python world' 'Name' Checking substring with help operator print('Substring found') s.find('Name') Checking substring with help find() print('Substring found') Here, find() method returns index substring's position, matched, otherwise will return Example Code 'Welcome python world' 'name' Print ('Substring found') Substring found s.find('name') Print ('Substring found') Substring found Checking substring occurrence identify number iterations substring array using count method. Syntax syntax checking occurrence substring that lies string given below- Welcome python world print('Substring count s.count('c')) 'Python best language' print('Substring count s.count('Py')) Example code Welcome python world print('Substring count s.count('c')) 'Python best language' print('Substring count s.count('Py')) Output following output, after execution above code. indexes finding substring Python language does have built-in function array sub-string index values. After all, using find() method, simply achieve one. Syntax syntax finding indexes substring given below- find_all_indexes (input_str, substring): 'Python best programming language' print(find_all_indexes(s, 'Py')) Example code find_all_indexes(input_str, substring): length Len(input_str) index while index Length: input_str.find(substring, index) return L2.append(i) index return Python best programming language print(find_all_indexes(s, 'Py')) Output: After successful execution above program code, following output. Slicing with help start-index without end-index This gets back sliced string starting from position array until last python sequence. Syntax syntax slicing substring through start-index without end-index given below- startIndex Example code Substring slicing with help start index without index demonstrate substring functionality python.' [5:] Output After execution above code, below screen. Slicing with help end-index without start-index This returns sliced string from beginning end-index-1. Syntax syntax slicing substring end-index without start-index given below- endIndex Example code Substring slicing with help index without start index 'Python powerful programming language' [7:] Output After execution above code, below screen. Slicing with complete string helps whole string output. Syntax syntax slicing whole substring shown below- Example code Substring slicing complete string 'Python robust programming language.' Output After execution above code, below screen. Slicing single character from string This gets back single character substring from string. Syntax syntax slicing single character from string shown below- index Example code Substring slicing single character 'Python widely used language.' Output After successful execution above code, following output. Reverse string with help negative step will help return string reverse order. Syntax syntax slicing reverse substring string shown below- negative value Example code Reversing string with help Substring slicing through negative step 'Python language supports string concept.' Output After successful execution above program code, below output. Working negative index working functionality negative index demonstrated below table- Here, above table, take word Python demonstrate exactly working functionality negative index. Slicing string with help negative index used slice substring string with help negative index. sequence index begins from we'll utilize negative index well. Syntax syntax slicing string with help negative index shown below- negative value Example code Substring slicing string through negative index 'PYTHON' Output After successful execution above program code, below output. Slicing string with help positive index used slice substring string with help positive index. Syntax syntax slicing string with help positive index shown below- positive value Example code Substring slicing string through positive index 'PYTHON' Output After successful execution above program code, below output. Retrieving substring with help list comprehension returns complete substrings string with help string slicing list comprehension. Example code Using slicing list comprehension string's substring 'PYTHON' range (Len(s)) 	for range Len(s) print (str) Output After successful execution above program code, below output. Retrieving substring with help itertools.combination() method returns complete substrings string with help string slicing list comprehension. Example code Using slicing itertools.combination() string's substring 'PYTHON' range (Len(s)) 	for range Len(s) print (str) Output After successful execution above program code, below output. Next TopicProject ideas Python Beginners prev next