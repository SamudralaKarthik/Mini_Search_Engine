next prev Python Arrays array defined collection items that stored contiguous memory locations. container which hold fixed number items, these items should same type. array popular most programming languages like C/C++, JavaScript, etc. Array idea storing multiple items same type together makes easier calculate position each element simply adding offset base value. combination arrays could save time reducing overall size code. used store multiple values single variable. have list items that stored their corresponding variables like this: car1 "Lamborghini" car2 "Bugatti" car3 "Koenigsegg" want loop through cars find specific one, array. array handled Python module named array. useful when have manipulate only specific data values. Following terms understand concept array: Element Each item stored array called element. Index location element array numerical index, which used identify position element. Array Representation array declared various ways different languages. important points that should considered follows: Index starts with access each element index. length array defines capacity store elements. Array operations Some basic operations supported array follows: Traverse prints elements one. Insertion adds element given index. Deletion deletes element given index. Search searches element using given index value. Update updates element given index. Array created Python importing array module python program. from array import arrayName array(typecode, [initializers]) Accessing array elements access array elements using respective indices those elements. import array arr.array('i', print("First element:", a[0]) print("Second element:", a[1]) print("Second last element:", a[-1]) Output: First element: Second element: Second last element: Explanation: above example, have imported array, defined variable named that holds elements array print elements accessing elements through indices array. change elements Arrays mutable, their elements changed similar like lists. import array numbers arr.array('i', 10]) changing first element numbers[0] print(numbers) Output: array('i', 10]) changing element numbers[2:5] arr.array('i', print(numbers) Output: array('i', 10]) Output: array('i', 10]) array('i' ,[0, 10]) Explanation: above example, have imported array defined variable named "numbers" which holds value array. want change elements array, defining particular index array where want change elements. arrays Python? combination arrays saves time. array reduce overall size code. delete elements from array? elements deleted from array using Python's statement. want delete value from array, that using indices particular element. import array number arr.array('i', number[2] removing third element print(number) Output: array('i', Output: array('i', [10, 60]) Explanation: above example, have imported array defined variable named "number" which stores values array. Here, using statement, removing third element given array. Finding length array length array defined number elements present array. returns integer value that equal total number elements present that array. Syntax len(array_name) Array Concatenation easily concatenate arrays using symbol. Example a=arr.array('d',[1.1 2.1 ,3.1,2.6,7.8]) b=arr.array('d',[3.7,8.6]) c=arr.array('d') c=a+b print("Array ",c) Output: Array c= array('d', [1.1, 2.1, 3.1, 2.6, 7.8, 3.7, 8.6]) Explanation above example, have defined variables named "a, c" that hold values array. Example import array arr.array('i', 22]) print("First element:", x[0]) print("Second element:", x[1]) print("Second last element:", x[-1]) Output: First element: Second element: Second last element: Explanation: above example, first, have imported array defined variable named which holds value array then, have printed elements using indices array. Next TopicPython Tutorial prev next