next prev create virtual environment Python Python application will often packages additional modules that don't come part standard library. Sometimes applications require specific version library perform specific task fixed bug. importance creating virtual environment Suppose developing applications that require older version library, other applications require version libraries same Python installation. Sometimes, leads problem. other words, Python installation able meet requirements every application. requirement various applications create conflict with each other. example, application requires version another application requires version 2.0, they will create conflict, result application unable run. resolve this conflicting requirement, Python offers create virtual environment. Application environment with version 1.0. Application environment with version 2.0, want upgrade application with version 3.0, this will affect application environment. Creating Virtual Environment module venv used create manage virtual environment. available with most recent version Python. decide location create virtual environment venv module script with directory path. Type following command your command line enter button. python3 venv new-env above command will create new-env directory; also creates directory inside newly created virtual environment new-env, containing copy Python interpreter. Note: write simply python instead python3, because used only have installed various versions Python. virtual environments stored common directory location venv folder. Once create virtual environment, have activate typing following command. Window, Press enter after typing new-env\Scripts\activate.bat UNIX MacOs, run: source new-env/bin/activate After activating virtual environment, will change shell prompt's tell what virtual environment using. Managing Packages Using install, upgrade, delete libraries activated virtual environment using Python package manager called pip. example (new-env) install requests==2.6.0 Collecting requests==2.6.0 Using cached requests-2.6.0-py2.py3-none-any.whl Installing collected packages: requests Successfully installed requests-2.6.0 re-run previous command, will display required version already installed. Next TopicHow declare variable Python prev next