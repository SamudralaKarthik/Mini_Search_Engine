next prev Wordcloud Package Python Wordcloud package helps know frequency word textual content using visualization. implement this need install some packages first, like pandas, matplotlib, Wordcloud. have look steps installation each- Installation Pandas Pandas great tool implement data analysis visualizations Jupyter Notebook. imported into source code following way- import pandas refers process aliasing with help which data frames created makes readability code easier. Pandas installed using ways- Using Command Prompt First, let's work objective using command prompt. Open Command Prompt Type given command- install pandas clicking 'Enter', packages will start downloading system. same command used Linux terminal install pandas system. Using Anaconda Navigator second install pandas system using Anaconda Navigator Open Anaconda Navigator. Click 'Environment' create option pandas your system. Click create button Pandas environment. list packages, select 'All' filters. search look 'Pandas' select 'Pandas package'. Right-click checkbox 'Mark specific version installation'. Select version want install click 'Apply' button install packages. Installation Matplotlib Matplotlib vast interesting library people enthusiastic about inferring results from data there scatterplots, histograms, boxplots, more which makes easy comprehend. Matplotlib installed following given steps- Using Command Prompt Matplotlib installed system using given command Command Prompt- install matplotlib Using Anaconda install matplotlib using Anaconda typing following command Anaconda Prompt- conda install matplotlib Verifying Installation verify that matplotlib been successfully installed system typing given program terminal- import matplotlib matplotlib.__version__ Installation Wordcloud discussed earlier, gives idea most occurred words text with help visual. have look steps installation- WordCloud installed following given steps- Using Command Prompt WordCloud installed system using given command command prompt. install wordcloud Using Anaconda install wordcloud using Anaconda typing following command Anaconda Prompt. conda install conda-forge wordcloud have look simple program that shows wordcloud used Python. have taken this piece text from website save sunflowers1.txt file. sunflowers1.txt "Sunflowers heliotropic, which means that they turn their flowers follow movement across east west, then returns night face east, ready again morning sun. Heliotropism happens during earlier stages before flower grows heavy with seeds. There tons varieties sunflowers available today, there's bound that fits your garden. Choose between those with branching stems single stems, those that produce ample pollen pollinators pollen-free (best bouquets), those that stay small tower above rest garden, those that produce edible seeds! Code Implementation import import matplotlib.pyplot from wordcloud import WordCloud, STOPWORDS text open("/content/sunflowers1.txt", "r").read() Clean text text re.sub(r'==.*?==+', text) text text.replace('\n', Define function plot word cloud plot_cloud(wordcloud): figure size plt.figure(figsize=(40, 30)) Display image plt.imshow(wordcloud) axis details plt.axis("off") Generate word cloud wordcloud WordCloud(width 3000, height 2000, random_state=1, background_color='salmon', colormap='Pastel1', collocations=False, stopwords STOPWORDS).generate(text) plot_cloud(wordcloud) Output: Next TopicConvert dataframe into list prev next