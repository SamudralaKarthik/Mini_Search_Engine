next prev Script Deployment Windows This section tutorial illustrates python script will deployed start-up that don't need open terminal time script. Lets first change hosts file path from "/etc/hosts" "C:\System32\drivers\etc\hosts" hosts file stored this location windows. schedule tasks windows, need open task scheduler shown below image. Click Create Task.. given right pane application. following window will open. Configure properties give name other required properties your script. check Checkbox "Run with highest privileges". Triggers create trigger shown below image. Select "At startup" option from drop-down list that script start-up. Now, Actions create action clicking new. following window will open. Select "Start program" action from drop-down list browse path script, i.e. blocker.py case E:\blocker.py) click Now, click Conditions deselect option which says "Start task only computer AC power." Now, settings click shown following image. Now, task Website-blocker scheduled system start-up. check this task list shown following image. Now, need restart system make script active system start-up. Restart system now, access blocked website www.facebook.com working hours now. will show display which looks like following. Hence, have script working fine system start-up block access www.facebook.com website want) automatically. Next TopicNumPy Tutorial prev next