next prev Connect SQLite with Python First have install Python SQLite your syatem. Install Python following code: sudo apt-get update sudo apt-get upgrade python Press installation will completed within seconds. Install SQLite Installation steps type following command: sudo apt-get install sqlite3 libsqlite3-dev After installation check installation, sqlite terminal will give prompt version info sqlite3 desired folder create database: sqlite3 database.db It'll create database.db folder you've given command. check your database created, following command sqlite3 terminal: .databases Note: connect SQLite with Python, need install connection module separately because being shipped default along with Python version 2.5.x onwards. SQLite with Python Create python file "connect.py", having following code: #!/usr/bin/python import sqlite3 conn sqlite3.connect('javatpoint.db') print "Opened database successfully"; Execute following statement command prompt: python connect.py connection created with javatpoint database. create table. Create table Create table "Employees" within database "javatpoint". Create python file "createtable.py", having following code: #!/usr/bin/python import sqlite3 conn sqlite3.connect('javatpoint.db') print "Opened database successfully"; conn.execute('''CREATE TABLE Employees (ID PRIMARY KEY NULL, NAME TEXT NULL, NULL, ADDRESS CHAR(50), SALARY REAL);''') print "Table created successfully"; conn.close() Execute following statement command prompt: python createtable.py table "Employees" created "javatpoint" database. Insert Records Insert some records "Employees" table. Create python file "connection.py", having following code: #!/usr/bin/python import sqlite3 conn sqlite3.connect('javatpoint.db') print "Opened database successfully"; conn.execute("INSERT INTO Employees (ID,NAME,AGE,ADDRESS,SALARY) VALUES 'Ajeet', 'Delhi', 20000.00 )"); conn.execute("INSERT INTO Employees (ID,NAME,AGE,ADDRESS,SALARY) VALUES 'Allen', 'London', 25000.00 )"); conn.execute("INSERT INTO Employees (ID,NAME,AGE,ADDRESS,SALARY) VALUES 'Mark', 'CA', 200000.00 )"); conn.execute("INSERT INTO Employees (ID,NAME,AGE,ADDRESS,SALARY) VALUES 'Kanchan', 'Ghaziabad 65000.00 )"); conn.commit() print "Records inserted successfully"; conn.close() Execute following statement command prompt: python connection.py Records inserted successfully. Select Records fetch display your records from table "Employees" using SELECT statement. Create python file "select.py", having following code: #!/usr/bin/python import sqlite3 conn sqlite3.connect('javatpoint.db') data conn.execute("select from Employees"); data: print "ID row[0] print "NAME row[1] print "ADDRESS row[2] print "SALARY row[3], "\n" conn.close(); Execute following statement command prompt: python select.py records have inserted before. same procedures, update delete table SQLite database usnig Python. Next TopicSQLite Interview Questions prev next