next prev Python Nmap Module know about what ports they associated with URLs, many have ever heard about port scanner. Basically, port scanner application that designed probing host server connected with particular port, used servers hosts open ports. From last line, also conclude that cannot port scanner applications closed ports without their special permission, illegal perform scanning closed ports without permission. Port scanners usually used administrative officials verify security policies their networks check these networks will identify attackers various threats. Note: Open ports ports that require special permission from host inspect their server port using port scanner. Python diversified programming language which means applications Python language almost every field task associated with technology. Therefore, question that will surely come mind that possible that build port scanner using Python more question, which build port scanner ourselves. build port scanner application, this even using Python program. Yes, absolutely true that build port scanner application using Python program, even scan multiple ports using application built. Python provides with modules that Python program build port scanners. such modules that find Python nmap-python module, used build port scanners. going learn about Nmap module this article, will build port scanner using this module Python program. Nmap Module Python Nmap Module, whose actual name nmap-python Module, open-source Python module that helps achieve task port scanning creating port scanner using Python program. using this Nmap Module Python program, build port scanner, when program, port scanner will automatically inspect port ranges defined inside program. Nmap: free open-source network scanning tool that used create port scanners performing inspection multiple ports known Nmap. Nmap Module: Installation Python Nmap Module inbuilt module Python, want create port scanner using this module Python, have first install system. There multiple ways through which install this Nmap Module system, will prefer installer achieve this task. Therefore, will installer from command prompt terminal install Nmap Module device that import into Python program. will following installer command command prompt terminal device that install Nmap Module system: install python-nmap After writing command shell did, have press enter start installation process have wait while until installation process complete. that installation process Nmap Module completed, successfully installed system. Nmap Module function inside Python program importing this module into program. Nmap Module: Implementation will this Nmap Module Python program, with help functions provided this module, will create port scanner using program. have already learned that doing port scanning closed ports without permission illegal, therefore will port scanner choosing localhost (127.0.0.1) target. this example Python program, will give range port scanner, program will tell which port given range open which port closed. Example: Look following example Python program where have used Nmap Module perform port scanning localhost (127.0.0.1): Import Nmap Module import nmap Providing range port scanning starting closing Assigning target host targetHost '127.0.0.1' Using scanner from Nmap Module portScanner nmap.PortScanner() Looping over range check each port range(starting, closing Scanning target host resultant portScanner.scan(targetHost, str(j)) Storing result form dictionary resultant resultant['scan'][targetHost]['tcp'][j]['state'] Printing result port print(f'The port {j} number from range {resultant}.') Output: port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. port number range closed. Explanation: have first imported Nmap Module into program that port scanner other functions program. After that, have defined range which will scan each port check whether port (from range) closed open, will this with every port present range. After that, have defined target host performing inspection, initialized 'targetHost' variable open local host Then, used portscanner() function from Nmap Module initialize scanner variable which will scan target host later. After that, used loop that could loop over each port given range check closed open. Inside loop, first, have initialized 'resultant' variable where scanned target host using scanner variable initialized previously. After that, loop, again called resultant variable store result whether port closed open, used dictionary that contains multiple variables produce result. last, used print statement print result each port from range. This loop will continue until reaches ending point range, then program will completely executed. output, even that result separately printed each port check port closed open. That's Nmap Module Python program create port scanner scan multiple ports with Note: successfully program which going discuss this article, have install nmap tool system, want install this tool system, have refer official website nmap tool. From official website, download setup file nmap tool then install nmap tool system program discussed this article successfully. Next TopicPython PyLab Module prev next