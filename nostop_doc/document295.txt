next prev Automorphic Number Python number said automorphic number last digits square this number give same number. following examples automorphic numbers- Input: Output Yes, automorphic number. Reason square gives 625, since last digits automorphic number. Input: Output automorphic number. Reason square gives 196, since last digits automorphic number. Input: Output Yes, automorphic number. Reason square gives 6776, since last digits automorphic number. Since concept clear now, build logic check whether number automorphic not. know that modulus operator used perform functions digits number. following illustrates done Python. Example int(input("Enter number want check: \n")) #calculating number digits num_of_digits len(str(num)) #computing square number square num**2 #obtaining last digits last_digits square%pow(10,num_of_digits) #comparing digits number with input last_digits num: print("Yes, automorphic number".format(num)) else: print("No, automorphic number".format(num)) Output Enter number want check: Yes, automorphic number let's have glance step-by-step approach same- first step will take number from user calculate square. calculate number digits using function. next thing calculate square number. Now, will make power function modulus operator obtain last digits. Finally, will compare last digit with input number. executing program, desired output will displayed. what happens when pass numbers that discussed example. Since automorphic number, displays required message. Output Enter number want check: Yes, automorphic number Since automorphic number, displays required message. Output Enter number want check: automorphic number Using While loop next approach same follows- Example print("Enter number want check:") num=int(input()) square=num*num flag=0 while(num>0): if(num%10!=square%10): print("No, automorphic number.") flag=1 break num=num//10 square=square//10 if(flag==0): print("Yes, automorphic number.") Output Enter number want check: Yes, automorphic number. understand steps have followed this program- first step remains same, that take number from user calculate square. have declared while loop which will execute till number becomes zero. will compare whether units digit number equal unit place number obtained after computing square. above condition satisfied, then will floor division number squared number. executing program, displays whether number automorphic number not. this article, learned what automorphic number check whether given number automorphic using Python. Next Topicsizeof Python prev next