next prev Applying Lambda functions Pandas Dataframe What Dataframes? data stored form rows columns two-dimensional data then generally called data frames Pandas. Pandas, function column with help lambda function. Syntax: lambda_Argument expression Example1: Using Dataframe.assign(), apply lambda function single column. import pandas from IPython.display import display values= [['Ayush',455],['Gopal',250],['Ankit',495], 		['Neha',400],['Nitin',350],['Shivani',450]] dataFrame pd.DataFrame(values,columns=['Name','MarksObtained']) dataFrame dataFrame.assign(Percentage lambda (x['MarksObtained'] /500 100)) display(dataFrame) Output: Explanation: above code, have dataframe where there columns with rows where column represents name student, another column represents marks obtained. Using assign() function, have added column name percentage, values this column defined using lambda functions. inserted percentage dividing marks multiplying 100. have applied lambda function single column marks obtained only. Example Applying lambda function more than column: import pandas from IPython.display import display valuesList [[13, 3.5, 100], [19, 4.6, 40], [23, 4.2, 69], 			[54, 8.5, 84], [32, 3.6, 70], [14, 4.6, 56], 			[15, 8.3, 109]] dataFrame pd.DataFrame(valuesList, columns=['Column1', 'Column2', 'Column3']) dataFrame dataFrame.assign(ProductOfColumns=lambda (x['Column1'] x['Column2'] x['Column3'])) display(dataFrame) Output: Explanation: above code, have three columns which represent some values. With assign() function, added column, values column calculated using lambda function multiplying values three columns. multiple columns time using lambda function. Example3: have applied lambda function columns, apply lambda function rows also. With apply() function, apply lambda function single row. import pandas from IPython.display import display import numpy valuesList [[13, 3.5, 100], [19, 4.6, 40], [23, 4.2, 69], 			[54, 8.5, 84], [32, 3.6, 70], [14, 4.6, 56], 			[15, 8.3, 109]] dataFrame pd.DataFrame(valuesList, columns=['Column1', 'Column2', 'Column3'],index=['a','b','c','d','e','f','g']) dataFrame dataFrame.apply(lambda np.square(x) x.name else axis=1) display(dataFrame) Output: Explanation: above code, have dataframe which contains rows. have assigned index names each row. Using lambda function, taking square values whose index value 'e'. code, values fifth column squared. Example have applied lambda function single row, apply multiple rows time. will apply function implement lambda function. import pandas from IPython.display import display import numpy valuesList [[13, 3.5, 100], [19, 4.6, 40], [23, 4.2, 69], 			[54, 8.5, 84], [32, 3.6, 70], [14, 4.6, 56], 			[15, 8.3, 109]] dataFrame pd.DataFrame(valuesList, columns=['Column1', 'Column2', 'Column3'],index=['a','b','c','d','e','f','g']) dataFrame dataFrame.apply(lambda np.square(x) x.name ['a', 'e', 'g'] else axis=1) display(dataFrame) Output: Explanation: above code, have applied lambda function multiple rows using DataFrame.apply() function. squared values rows whose index value 'a', 'g'. output, these three rows their square values. Example have seen, have applied lambda function either columns. also apply lambda function both rows columns simultaneously using function apply() assign(). import pandas from IPython.display import display import numpy valuesList [[13, 3.5, 100], [19, 4.6, 40], [23, 4.2, 69], 			[54, 8.5, 84], [32, 3.6, 70], [14, 4.6, 56], 			[15, 8.3, 109]] dataFrame pd.DataFrame(valuesList, columns=['Column1', 'Column2', 'Column3'],index=['a','b','c','d','e','f','g']) dataFrame dataFrame.apply(lambda np.square(x) x.name ['a', 'b', 'g'] else axis=1) dataFrame dataFrame.assign(Sum=lambda (x['Column1'] x['Column2'] x['Column3'])) display(dataFrame) Output: Explanation: above code, have applied lambda function rows columns single dataframe. code, first, have applied lambda function rows using apply() function, which will square values some rows. Then applied lambda function three columns using assign() function, which will column which contains columns. first, values will squared, then they will added fourth column. Next TopicBox Plot Python using Matplotlib prev next