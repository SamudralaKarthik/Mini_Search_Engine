next prev Python random randrange() Python random module allows generating random numbers. generated numbers sequence pseudo-random numbers, which based used function. There different types functions used random module generate random numbers, such random.random(), random.randint(), random.choice(), random.randrange(start, stop, width), many more. Here, will discuss randrange() function random module. randrange() function used generate random number between specified range parameter. accepts three parameters: starting number, stop number, width, which used skip number range. Syntax random.randrange() random.randrange(start, stop[, width]) random.randrange() function returns random integer number within given range, i.e., start stop. random.randrange() function takes three parameters input start, stop, width. these three parameters, parameters start width optional. start parameter starting lower limit integer number random range. start parameter passed parameter, takes default value (zero). stop parameter last number upper limit integer number random range. width parameter range between each number random sequence. This parameter optional, width parameter passed parameter, takes default value (one). randrange(start, stop, width) function does include parameter while generating random integer number. stop parameter exclusive, gets generated random number. Look this random.randrange(3, function that will generate random integer value between like …27. this function never includes while generating random number. Generating random application always been important, several real-time uses day-to-day life. example, generating sending random number customer (one-time-password) making safe transaction. Another example generating random number used while playing ludo game generate random dice number. Examples random.randrange() generate random integers number within given range Let's example where generating random integer number within given range. This example shows different forms random.randrange() function. import random print("Generating random number within given range Random number between number1 random.randrange(30) print("Random integer: number1) Random number between number2 random.randrange(10, print("Random integer: number2) Random number between divisible number3 random.randrange(25, 201, print("Random integer: number3) Output: Example Generate random integer number range (multiple) Let's generate random integer number between 100, which range such 95. import random #print "Random number range (multiple) 5" number random.randrange(5, 100, print("Random number range (multiple) number) Output: Example Generate random integer number specific length also generate random integer number specific length. want generate random number length input start stop parameter randrange() function least number digit length (100) least number digit length (1000) because generates random number digit form where digits (1000) excluded. Example: Generate random number digit using random.randrange() import random number1 random.randrange(100, 1000) print("First random number length number1) number2 random.randrange(100, 1000) print("Second random number length number2) Output: First random number length 813 Second random number length 770 Example Generate random negative integer number: Let's example that generates random negative integer number between -10. import random randomNagativeInteger random.randrange(-50, -10) print("Random negative integer number between -10") print("Random negative integer number between ",randomNagativeInteger) Output: Random negative integer number between Random negative integer number between -43 Example Generate random positive negative integer number import random number1 random.randrange(-5, print(number1) number2 random.randrange(-5, print(number2) Output: Note that parameter start stop, which passed random.randrange() function must increasing order, such random.randrange(5, random.randrange(-20, -10). start parameter always smaller than stop parameter; otherwise, this function generates error "Traceback (most recent call last) raise ValueError empty range randrange()". example: Look this example which violating rule passing start parameter greater than stop parameter random.randrange() function, which generates error message import random randomNumber random.randrange(10,5) Output: Some other functions Python's random module that used generate numbers randomly are: choice() randint() sample() sort() random.choice() Python random module choice() function used choose random element from list other sequence. random.choice() function used returns random element from list values. Syntax ramdom.choice() random.choice(sequence) random.choices(sequence, k=l) Here parameter sequence string, list, tuple, random.choice() only single random element. random.choices() function number elements returned. Suppose don't mention value parameter random.choices() returns single item from sequence list. Example this example, providing sequence (list) parameter returning single random element. import random listValue [5,10,15,20,25,30] item random.choice(listValue) print("random item from list item) Output: random item from list Example random.choices() function mostly used returning random elements with various possibilities. This function also takes weight number random choices. example, want print random movies name from list movies. import random movieList ['Matrix', 'Terminator', 'Black Panther', 'Avengers', 'Titanic'] randomMovies random.choices(movieList, k=3) print("random movies from list are: randomMovies) Output: random movies from list are: ['Avengers', 'Black Panther', 'Titanic'] random.randint() Python random.randint() function used generate random integer number within specified range. Syntax: random.randint(start, stop) random.randint() function takes parameters start, which starting range, stop, which range. Both parameters (start, stop) included while generating random number. Example random.randint() generating list random integer This example will create list multiple random integers using randint() function. Generating list random integers between 100, both values inclusive. import random randomListValue specifing length list equal range(0, generates random numbers from randomListValue.append(random.randint(1, 100)) print("Printing list generated random numbers") print(randomListValue) Output: Printing list generated random numbers [65, However, there possibility that random.randint() function returns duplicate number output result. avoid duplicate random integer number result, random.sample() function. random.sample() above example, there possibility that random.randint() generates duplicate random number from list. want generate unique random numbers from list, random.sample() function. random.sample() function generates unique elements from set, list, other sequences. Python's random.sample() function allows random sampling more than element within range from list without duplicating. Example random.sample() generate random elements from list without duplicates: this example, will generate random integers from range 100. import random Generate unique random numbers within range randomList random.sample(range(0, 100), print(randomList) Output: [15, want generate list random numbers each them must multiple 'n', then step parameter range() function. example, generating random integers which multiple from range using random.sample(range(0, 100, 10). import random Generate unique random numbers within range randomList random.sample(range(0, 100, print(randomList) Output: [75, List sort() Python sort() function used sort random integer numbers list ascending order default). also sort list value descending order specifying order reverse True. default value reverse False. Example sort() sorting list elements ascending order this example, will generate random list numbers range with width sort them using sort() function. import random randomList random.sample(range(50, 100, print("Before sorting random integers list") print(randomList) sort list ascending order default randomList.sort() print("After sorting random integers list") print(randomList) Output: Before sorting random integers list [90, After sorting random integers list [55, Example sort() sorting list elements descending order this example, will sort list elements descending order using randomList.sort(reverse=True). import random randomList random.sample(range(50, 100, print("Before sorting random integers list") print(randomList) sort list descending order randomList.sort(reverse=True) print("After sorting random integers list") print(randomList) Output: Before sorting random integers list [70, After sorting random integers list [90, Next TopicPermutation Combination Python prev next