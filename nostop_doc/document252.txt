next prev Fraction Module Python Python Fraction module used work with arithmetic rational numbers. This module allows create Fractional instance integers, floats, numbers, decimal, strings. What Fractional Instance? Fractional instances created using pair integers, from string, another rational number. These hashable should treated mutable. create fractional instance? Fraction module provides following built-in functions that used create fraction. Let's understand following ways creating fraction instance. class fractions.Function(numerator denominator This methods assigns numerator denominator default. denominator equal will raise Zerodivision error. Let's understand following example. Example from fractions import Fraction print (Fraction(10, 37)) returns Fraction(11, print (Fraction(11, 18)) returns Fraction(5, print (Fraction()) returns Fraction(0, Output: 10/37 11/18 class fractions.Fraction(other_fraction) This method takes other_fraction instance number. returns rational fraction instance with same value. class fractions.Fraction(float) This method takes float value returns same value fraction instance. Example from fractions import Fraction print(Fraction(1/12)) Output: 6004799503160661/72057594037927936 class fractions.Fraction(decimal) This method takes decimal instance returns instance with same value. Let's understand following example. Example from fractions import Fraction print (Fraction('2.26')) Output: 113/50 class fractions.Fraction(string) This method takes string Unicode argument returns instance with same value. Let's understand following example. Example from fractions import Fraction print (Fraction('7/25')) print (Fraction('1.23')) print (Fraction('3/5')) print (Fraction('1.414213 \t\n')) Output: 7/25 123/100 1414213/1000000 limit_denominator(max_denominator=1000000) used find approximation given floating-point number. returns closet Fraction self that denominator max_denominator. Let's understand following example. Example from fractions import Fraction print (Fraction('3.14159265358979323846')) print (Fraction('3.14159265358979323846').limit_denominator(10000)) print (Fraction('3.14159265358979323846').limit_denominator(100)) print (Fraction('3.14159265358979323846').limit_denominator(10)) print (Fraction(125, 50).numerator) print (Fraction(125, 50).denominator) Output: 157079632679489661923/50000000000000000000 355/113 311/99 22/7 Example Perform Mathematical Operation Fractional from fractions import Fraction print (Fraction(56, Fraction(18, 25)) print (Fraction(5, Fraction(12, 10)) print (Fraction(16, Fraction(26, 39)) print (Fraction(18, Fraction(15, 36)) print (Fraction(22, Fraction(6, 10)) Output: 404/75 25/12 32/45 2.4326050606703427 Conclusion this tutorial, have discussed Fraction module important methods. These methods used complex mathematical computation machine learning project. Next TopicPopular Python Framework Build prev next