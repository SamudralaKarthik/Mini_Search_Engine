next prev Tkinter LabelFrame LabelFrame widget used draw border around child widgets. also display title LabelFrame widget. acts like container which used group number interrelated widgets such Radiobuttons. This widget variant Frame widget which features frame. also display label. syntax LabelFrame widget given below. Syntax LabelFrame(top, options) list options given below. Option Description background color widget. represents size border shown around indicator. default pixels. Class default value class LabelFrame. colormap This option used specify which colomap used this widget. colormap, mean colors that used form graphics. With this option, reuse colormap another window this widget. container this true, LabelFrame becomes container widget. default value false. cursor cursor type, i.e. arrow, dot, etc. mouse pointer changed cursor type when over widget. represents foreground color widget. font represents font type widget text. height represents height widget. labelAnchor represents exact position text within widget. default NW(north-west) labelwidget represents widget used label. frame uses text label value specified. highlightbackground color focus highlight border when widget doesn't have focus. highlightcolor color focus highlight when widget focus. highlightthickness width focus highlight border. padx horizontal padding widget. pady vertical padding widget. relief represents border style. default value GROOVE. text represents string containing label text. width represents width frame. Example !/usr/bin/python3 from tkinter import Tk() top.geometry("300x200") labelframe1 LabelFrame(top, text="Positive Comments") labelframe1.pack(fill="both", expand="yes") toplabel Label(labelframe1, text="Place positive comments") toplabel.pack() labelframe2 LabelFrame(top, text "Negative Comments") labelframe2.pack(fill="both", expand "yes") bottomlabel Label(labelframe2,text "Place negative comments") bottomlabel.pack() top.mainloop() Output: Next TopicTkinter messagebox prev next