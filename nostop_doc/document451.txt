next prev Ping Pong Game Using Turtle Python ping pong game most famous games arcadia, which quite similar table tennis. rules this game simple: There players. Both have control over paddle their side. They move paddle vertical sides from down down Players strike back forth ball using paddle. Turtle: turtle built-in module Python which used graphical illustrations. users panel illustrate graphics. following steps used illustrating Ping Pong game: Step user create Paddles "Left Side" "Right Side" screen. Step user then create ball. Step Then, create event moving paddles vertically pressing particular keys, that "Left Paddle": "Up" "Down", "Right Paddle": "Up" "Down "Down". Step last, create function updating score after each player misses strike ball. Code Creating Illustrating Ball Paddle: import import turtle First, will create screen screen_1 turtle.Screen() screen_1.title("Ping-Pong Game") screen_1.bgcolor("Yellow") screen_1.setup(width 1050, height 650) Left paddle left_paddle turtle.Turtle() left_paddle.speed(0) left_paddle.shape("square") left_paddle.color("Red") left_paddle.shapesize(stretch_wid stretch_len left_paddle.penup() left_paddle.goto(-400, Right paddle right_paddle turtle.Turtle() right_paddle.speed(0) right_paddle.shape("square") right_paddle.color("Blue") right_paddle.shapesize(stretch_wid stretch_len right_paddle.penup() right_paddle.goto(400, Ball circle shape hit_ball turtle.Turtle() hit_ball.speed(45) hit_ball.shape("circle") hit_ball.color("White") hit_ball.penup() hit_ball.goto(0, hit_ball.dx hit_ball.dy Output: Explanation: First, imported turtle library. Then wrote code background screen using turtle.Screen() function. created Left Right paddle using turtle.Turtle() function positioned them using goto() function. last, created ball using turtle.Turtle() function mentioned moving speed screen using speed() function other required parameters. illustration ball paddle screen ready. Code Complete Code Illustrating Ping Pong Game with Players. import import turtle First, will create screen screen_1 turtle.Screen() screen_1.title("Ping-Pong Game") screen_1.bgcolor("Yellow") screen_1.setup(width 1050, height 650) Left paddle left_paddle turtle.Turtle() left_paddle.speed(0) left_paddle.shape("square") left_paddle.color("Red") left_paddle.shapesize(stretch_wid stretch_len left_paddle.penup() left_paddle.goto(-400, Right paddle right_paddle turtle.Turtle() right_paddle.speed(0) right_paddle.shape("square") right_paddle.color("Blue") right_paddle.shapesize(stretch_wid stretch_len right_paddle.penup() right_paddle.goto(400, Ball circle shape hit_ball turtle.Turtle() hit_ball.speed(45) hit_ball.shape("circle") hit_ball.color("Black") hit_ball.penup() hit_ball.goto(0, hit_ball.dx hit_ball.dy Now, will initialize score left_player right_player Displaying score sketch_1 turtle.Turtle() sketch_1.speed(0) sketch_1.color("blue") sketch_1.penup() sketch_1.hideturtle() sketch_1.goto(0, 260) sketch_1.write("Left Player Right Player: align "center", font ("Courier", "normal")) Implementing functions moving paddle vertically paddle_L_up(): left_paddle.ycor() left_paddle.sety(y) paddle_L_down(): left_paddle.ycor() left_paddle.sety(y) paddle_R_up(): right_paddle.ycor() right_paddle.sety(y) paddle_R_down(): right_paddle.ycor() right_paddle.sety(y) Then, binding keys moving paddles down. screen_1.listen() screen_1.onkeypress(paddle_L_up, "r") screen_1.onkeypress(paddle_L_down, "c") screen_1.onkeypress(paddle_R_up, "Up") screen_1.onkeypress(paddle_R_down, "Down") while True: screen_1.update() hit_ball.setx(hit_ball.xcor() hit_ball.dx) hit_ball.sety(hit_ball.ycor() hit_ball.dy) Check borders hit_ball.ycor() 280: hit_ball.sety(280) hit_ball.dy hit_ball.ycor() -280: hit_ball.sety(-280) hit_ball.dy hit_ball.xcor() 500: hit_ball.goto(0, hit_ball.dy left_player sketch_1.clear() sketch_1.write("Left_player Right_player: {}".format( left_player, right_player), align "center", font ("Courier", "normal")) hit_ball.xcor() -500: hit_ball.goto(0, hit_ball.dy right_player sketch_1.clear() sketch_1.write("Left_player Right_player: {}".format( left_player, right_player), align "center", font ("Courier", "normal")) Collision ball paddles (hit_ball.xcor() hit_ball.xcor() 370) (hit_ball.ycor() right_paddle.ycor() hit_ball.ycor() right_paddle.ycor() 40): 				hit_ball.setx(360) 				hit_ball.dx (hit_ball.xcor() -360 hit_ball.xcor() -370) (hit_ball.ycor() left_paddle.ycor() hit_ball.ycor() left_paddle.ycor() 40): hit_ball.setx(-360) hit_ball.dx Output: Explanation: above code, after illustrating outlook screen with paddles ball, will first initialize score both players, that Zero. Then, will display score screen, which will increase with opposite player missing strike. will then implement functions moving paddle vertically. will bind keys each function, that "Left Paddle": "Up" "Down", "Right Paddle": key" "Up" "Down key" "Down". Then, will initiate ball striking paddle will work, ball paddle, will opposite player, paddle missed ball, then ball will border, opposite player will score. code correct error found, screen Ping Pong Game will after running code, players play. ball will first right player. Conclusion this tutorial, have created simple illustration ping pong game screen using built-in library Python, that Turtle. user create different moving illustrations using turtle library different functions. Next TopicPython Function Display Calendar prev next