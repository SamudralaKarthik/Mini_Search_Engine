next prev Convert float Python have used different numeric data types Python, this tutorial will learn convert float value integer value. Let's have look approaches implement same- Using trunc() Using floor() Using ceil() Using int() let's started with first one- Using trunc() following program shows trunc() convert float value integer Python. #import trunc from math import trunc #initialising values 20.33 12.46 9.54 res_sum #displaying value print("The result res_sum) #using trunc print("The converted value trunc(a)) print("The converted value trunc(b)) print("The converted value trunc(c)) print("The converted value trunc(res_sum)) Output: result 42.33 converted value converted value converted value converted value Explanation- It's time understand what happened above program- Since have trunc(), have imported math first step. After this, have initialized three float values then calculated their sum. Finally, have passed variables res_sum trunc() obtain integer values. executing program, obtain desired output. next program, will make floor(). Using floor() First, let's understand what happens when pass float value floor()? When float number passed floor(), rounds number down nearest integer. Consider program given below, #import floor from math import floor #initialising values 20.33 12.46 9.54 res_sum #displaying value print("The result res_sum) #using floor print("The converted value floor(a)) print("The converted value floor(b)) print("The converted value floor(c)) print("The converted value floor(res_sum)) Output: result 42.33 converted value converted value converted value converted value Explanation- Let's have glance explanation this program. Since have floor(), have imported math first step. After this, have initialized three float values then calculated their sum. Finally, have passed variables res_sum floor() obtain integer values. executing program, obtain desired output. Now, shall ceil() used same. Using ceil() First, let's understand what happens when pass float value ceil()? When float number passed ceil(), rounds number nearest integer. #import ceil from math import ceil #initialising values 20.33 12.46 9.54 res_sum #displaying value print("The result res_sum) #using ceil print("The converted value ceil(a)) print("The converted value ceil(b)) print("The converted value ceil(c)) print("The converted value ceil(res_sum)) Output: result 42.33 converted value converted value converted value converted value Explanation- Let's understand what have done this program. Since have ceil(), have imported math first step. After this, have initialized three float values then calculated their sum. Finally, have passed variables res_sum ceil() obtain integer values. executing program, obtain desired output. Finally, last program, will most basic approach converting float value integer which using int(). Using int() program given below illustrates used program. #initialising values 20.33 12.46 9.54 res_sum #displaying value print("The result res_sum) #using int() print("The converted value int(a)) print("The converted value int(b)) print("The converted value int(c)) print("The converted value int(res_sum)) Output: result 42.33 converted value converted value converted value converted value Explanation- have initialized three float values then calculated their sum. Finally, have passed variables res_sum int() obtain integer values. executing program, obtain desired output. Conclusion this tutorial, learned interesting methods converting float integer Python. Next TopicMultiply Elements list Python prev next