next prev Matplotlib legend subplot this tutorial, will learn Matplotlib used include legends subplots. legend added after creating plot using legend() function. Syntax: syntax legend subplot axes[position].legend(loc where, used location. Approaches: Following approaches will displaying legend subplot Matplotlib: First, will subplots() function creating subplots bigger plot. Then, will legend() function adding label curves. last, will show plots using show() Example this example, will plot scatter graph using subplot log10 exponential: First, will import required modules from matplotlib import pyplot PPlt import numpy here will assign value axis x_axis1 nmp.arange(2, 1.5) Now, will value log10 y_axis_log10_1 nmp.log10(x_axis) then, will value exponential y_axix_exp1 nmp.exp(x_axis) Here, will create subplots using subplot() function fig, axes PPlt.subplots(2) now, will depicte visualization axes[0].plot(x_axis1, y_axis_log10_1, color 'Red', label "log10") axes[1].plot(x_axis1, y_axix_exp1, color 'Pink', label "exponential") Here, will select position which legend added axes[0].legend(loc 'best') axes[1].legend(loc 'best') last, will display plot PPlt.show() Output Example this example, will plot scatter graph using subplot sine cos: First, will import required modules from matplotlib import pyplot PPlt import numpy here will assign value axis x_axis1 nmp.arange(-2, 0.5) Now, will value sine y_axis_sine1 nmp.sin(x_axis) Now, will value y_axix_cose1 nmp.cos(x_axis) Here, will create subplots using subplot() function fig, axes PPlt.subplots(2) now, will depicte visualization axes[0].scatter(x_axis1, y_axis_sine1, color 'Red', marker '*', label "sine") axes[1].scatter(x_axis1, y_axix_cose1, color 'Black', marker '*', label "cos") Here, will select position which legend added axes[0].legend(loc 'best') axes[1].legend(loc 'best') last, will display plot PPlt.show() Output Example this example, will plot scatter graph using subplot x^2) x^3): First, will import required modules from matplotlib import pyplot PPlt here will assign value axis x_axis list(range(-15, 15)) here, will value y_axis_1 x_axis] here, will value y_axix_2 x_axis] Now, will create subplots using subplot() function fig, axes PPlt.subplots(2) Now, will depicte visualization axes[0].scatter(x_axis, y_axis_1, color 'Blue', marker '*', label axes[1].scatter(x_axis, y_axix_2, color 'Brown', marker '*', label Here, will select position which legend added axes[0].legend(loc 'upper center') axes[1].legend(loc 'upper left') last, will display plot PPlt.show() Output Conclusion this tutorial, have discussed legend subplot matplotlib graphs using different methods. Next TopicMorphological Operations Image Processing Python prev next