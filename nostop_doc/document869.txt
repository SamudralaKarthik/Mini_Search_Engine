next prev Python Tkinter Label Label used specify container where place text images. This widget used provide message user about other widgets used python application. There various options which specified configure text part text shown Label. syntax Label given below. Syntax Label (master, options) list possible options given below. Option Description anchor specifies exact position text within size provided widget. default value CENTER, which used center text within specified space. background color displayed behind widget. bitmap used bitmap graphical object specified that, label represent graphics instead text. represents width border. default pixels. cursor mouse pointer will changed type cursor specified, i.e., arrow, dot, etc. font font type text written inside widget. foreground color text written inside widget. height height widget. image image that shown label. justify used represent orientation text text contains multiple lines. LEFT left justification, RIGHT right justification, CENTER center justification. padx horizontal padding text. default value pady vertical padding text. default value relief type border. default value FLAT. text This string variable which contain more line text. textvariable text written inside widget control variable StringVar that accessed changed accordingly. underline display line under specified letter text. this option number letter under which line will displayed. width width widget. specified number characters. wraplength Instead having only line label text, break number lines where each line number characters specified this option. Example !/usr/bin/python3 from tkinter import Tk() top.geometry("400x250") #creating label uname Label(top, text "Username").place(x 30,y #creating label password Label(top, text "Password").place(x sbmitbtn Button(top, text "Submit",activebackground "pink", activeforeground "blue").place(x 120) Entry(top,width 20).place(x 100, Entry(top, width 20).place(x 100, top.mainloop() Output: Next TopicPython Tkinter Listbox prev next