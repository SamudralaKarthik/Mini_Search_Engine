next prev create DataFrames Python Data Frame two-dimension collection data. data structure where data stored tabular form. Datasets arranged rows columns; store multiple datasets data frame. perform various arithmetic operations, such adding column/row selection columns/rows data frame. import DataFrames from external storage; these storages referred Database, file, Excel file. also lists, dictionary, from list dictionary, etc. this tutorial, will learn create data frame multiple ways. Let's understand these different ways. First, need install pandas library into Python environment. empty dataframe create basic empty Dataframe. dataframe constructor needs called create DataFrame. Let's understand following example. Example import pandas import pandas Calling DataFrame constructor pd.DataFrame() print(df) Output: Empty DataFrame Columns: Index: Method Create dataframe using List create dataframe using single list list lists. Let's understand following example. Example importing pandas library import pandas string values list ['Java', 'Python', 'C', 'C++', 'JavaScript', 'Swift', 'Go'] Calling DataFrame constructor list dframe pd.DataFrame(lst) print(dframe) Output: Java Python JavaScript Swift Method Create Dataframe from dict ndarray/lists dict ndarray/lists used create dataframe, ndarray must same length. index will range(n) default; where denotes array length. Let's understand following example. Example import pandas assign data lists. data {'Name': ['Tom', 'Joseph', 'Krish', 'John'], 'Age': [20, 18]} Create DataFrame pd.DataFrame(data) Print output. print(df) Output: Name Joseph Krish John Method Create indexes Dataframe using arrays Let's understand following example create indexes dataframe using arrays. Example DataFrame using arrays. import pandas assign data lists. data {'Name':['Renault', 'Duster', 'Maruti', 'Honda City'], 'Ratings':[9.0, 8.0, 5.0, 3.0]} Creates pandas DataFrame. pd.DataFrame(data, index =['position1', 'position2', 'position3', 'position4']) print data print(df) Output: Name Ratings position1 Renault position2 Duster position3 Maruti position4 Honda City Explanation above code, have defined column name with various names their ratings. used array create indexes. Method Create Dataframe from list dicts pass lists dictionaries input data create Pandas dataframe. column names taken keys default. Let's understand following example. Example example create Pandas DataFrame lists dicts. import pandas assign values lists. data [{'A': 'B': 'C':30}, {'x':100, 'y': 200, 'z': 300}] Creates DataFrame. pd.DataFrame(data) Print data print(df) Output: 10.0 20.0 30.0 100.0 200.0 300.0 Let's understand another example create pandas dataframe from list dictionaries with both index well column index. Example import pandas assigns values lists. data [{'x': 'y': {'A': 'B': 'C': 19}] With column indices, values same dictionary keys dframe1 pd.DataFrame(data, index =['first', 'second'], columns =['x', 'y']) With column indices with index with other name dframe2 pd.DataFrame(data, index =['first', 'second'], columns =['x', 'y1']) print first data frame print (dframe1, "\n") Print second DataFrame. print (dframe2) Output: first second first second Let's understand another example create dataframe passing lists dictionary rows. Example example create Pandas DataFrame passing lists Dictionaries indices. import pandas assign values lists data [{'x': 'z':3}, {'x': 'y': 'z': 30}] Creates padas DataFrame passing Lists dictionaries index. dframe pd.DataFrame(data, index =['first', 'second']) Print dataframe print(dframe) Output: first second 20.0 have discussed three ways create dataframe using lists dictionary. Method Create Dataframe using zip() function zip() function used merge lists. Let's understand following example. Example example create pandas dataframe from lists using zip. import pandas List1 Name ['tom', 'krish', 'arun', 'juli'] List2 Marks [95, 47] lists. merge them using zip(). list_tuples list(zip(Name, Marks)) Assign data tuples. print(list_tuples) Converting lists tuples into pandas Dataframe. dframe pd.DataFrame(list_tuples, columns=['Name', 'Marks']) Print data. print(dframe) Output: [('john', 95), ('krish', 63), ('arun', 54), ('juli', 47)] Name Marks john krish arun juli Method Create Dataframe from Dicts series dictionary passed create dataframe. Dicts series where subsequent index union series passed index value. Let's understand following example. Example Pandas Dataframe from Dicts series. import pandas Initialize data Dicts series. {'Electronics' pd.Series([97, 45], index =['John', 'Abhinay', 'Peter', 'Andrew']), 'Civil' pd.Series([97, 96], index =['John', 'Abhinay', 'Peter', 'Andrew'])} creates Dataframe. dframe pd.DataFrame(d) print data. print(dframe) Output: Electronics Civil John Abhinay Peter Andrew this tutorial, have discussed different ways create DataFrames. Next TopicHow develop game Python prev next