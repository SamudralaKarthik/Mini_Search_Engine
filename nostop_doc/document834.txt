next prev Scatter() plot matplotlib Python Matplotlib Python library that lets make deterministic, animated, engaging visualizations. used make plots Python, such charts, scatter plots, charts, histograms, line plots, plots, many more. matplotlib library contains information about scatter plot. matplotlib.pyplot.scatter() Scatter plots utilized different variables related each other. dots plot shows variables related. scatter plot made with matplotlib library's scatter() method. People often scatter plots show relationship between more variables change affects other. Syntax Here's write code scatter() method: matplotlib.pyplot.scatter (x_axis_value, y_axis_value, None, None, vmin None, vmax None, marker None, cmap None, alpha None, linewidths None, edgecolors None) following syntax parameters scatter() method: x_axis_value array containing x-axis data scatter plot. y_axis_value array with y-axis data. size marker (can scalar array size equal size x-axis y-axis) order colors markers marker- marker style scatter plot python. cmap cmap name scatter plot python. Linewidths these size marker border scatter plot. edgecolor: border color marker scatter plot. Alpha blending value scatter fucntion, between (transparent opaque) parameters except x-axis data y-axis data optional, their default value None. Examples Here many examples scatter plots matplotlib with different parameters. different types scatter plots with size, color, other functions. Example1: following example shows scatter plot using Python. plot values y-axis position. import matplotlib.pyplot x_axis_value =[6, 	4, y_axis_value =[98, 	102, #plot basic scatter python plt.scatter(x_axis_value, y_axis_value) show scatter plot plt.show() Output following image shows scatter plot with value python. Example2: following example shows scatter plot using python. Here, numpy package compare plots. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) #plot basic scatter value python plt.scatter(x_axis_value, y_axis_value) x_axis_value np.array([5, 12]) y_axis_value np.array([101, 106, 105, 118, 85]) #plot basic scatter value python plt.scatter(x_axis_value, y_axis_value) plt.show() Output following image shows scatter plot with value python. Example3: following example shows scatter plot using python. Here, again used numpy package compare plots with different colors. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) #plot basic scatter function with color python plt.scatter(x_axis_value, y_axis_value, color 'red') x_axis_value np.array([5, 12]) y_axis_value np.array([101, 106, 105, 118, 85]) plt.scatter(x_axis_value, y_axis_value, color 'green') plt.show() Output following image shows scatter plot with value python. Example4: following example shows scatter plot using python. Here, different color each plotted value. assign color variable. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) colors_value np.array(["red", "cyan", "green", "yellow", "blue", "pink", "orange", "black", "purple", "beige", "magenta", "brown", "gray"]) plt.scatter(x_axis_value, y_axis_value, colors_value) plt.show() Output following image shows scatter plot with value python. Example5: following example shows scatter plot using Python. Here, different color each plotted value. "cmap" assign color variables color bars, respectively. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) colors_value np.array(["red", "cyan", "green", "yellow", "blue", "pink", "orange", "black", "purple", "beige", "magenta", "brown", "gray"]) plt.scatter(x_axis_value, y_axis_value, colors_value, cmap='flag') plt.colorbar() plt.show() Output following image shows scatter plot with value python. Example6: following example shows scatter plot using Python. Here, different sizes plot value. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) size_value np.array([90, 189, 101, 812, 1002, 391, 788, 660]) plt.scatter(x_axis_value, y_axis_value, size_value) plt.colorbar() plt.show() Output following image shows scatter plot with value python. Example7: following example shows scatter plot using python. Here different sizes opacity plot value. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) size_value np.array([90, 189, 101, 812, 1002, 391, 788, 660]) plt.scatter(x_axis_value, y_axis_value, size_value, alpha 0.6) plt.colorbar() plt.show() Output following image shows scatter plot with value python. Example8: following example shows scatter plot using python. Here, different color each plotted value. scatter plots with different colors, sizes, opacity. import matplotlib.pyplot import numpy x_axis_value np.array([4, y_axis_value np.array([90, 101, 102, 66]) size_value np.array([90, 189, 101, 812, 1002, 391, 788, 660]) colors_value np.array(["red", "cyan", "green", "yellow", "blue", "pink", "orange", "black", "purple", "beige", "magenta", "brown", "gray"]) plt.scatter(x_axis_value, y_axis_value, colors_value, size_value, alpha 0.6) plt.colorbar() plt.show() Output following image shows scatter plot with value python. Example9: following example shows scatter plot using python. Here, different color each plotted value. random scatter plots with different colors, sizes, opacity. import matplotlib.pyplot import numpy x_axis_val np.random.randint(50, size (50)) y_axis_val np.random.randint(50, size (50)) colors_value np.random.randint(50, size (50)) sizes_val np.random.randint(50, size (50)) plt.scatter(x_axis_val, y_axis_val, colors_value, sizes_val, alpha 0.6, cmap 'nipy_spectral') plt.colorbar() plt.show() Output following image shows scatter plot with value python. Example10: following example shows scatter plot using Python. Here, different color each plotted value. scatter with different colors, sizes, edges, line widths border. import matplotlib.pyplot import numpy x_axis_value np.array([4, 21]) y_axis_value np.array([90, 101, 102]) plt.scatter(x_axis_value, y_axis_value, color 'orange', 150, edgecolor 'blue', linewidth x_axis_value np.array([5, y_axis_value np.array([101, 106, 105, 98]) plt.scatter(x_axis_value, y_axis_value, color 'red', 300, edgecolor 'green', linewidth plt.show() Output following image shows scatter plot with value python. Conclusion matplotlib displays plot figures using Python language. scatter function python shows different type figures graph. figure shows different sizes, opacities python. Next TopicData Analysis Project Ideas Python prev next