next prev Python Tkinter Radiobutton Radiobutton widget used implement one-of-many selection python application. shows multiple choices user which, user select only them. associate different methods with each radiobutton. display multiple line text images radiobuttons. keep track user's selection radiobutton, associated with single variable. Each button displays single value that particular variable. syntax Radiobutton given below. Syntax Radiobutton(top, options) Option Description activebackground background color widget when focus. activeforeground font color widget text when focus. anchor represents exact position text within widget widget contains more space than requirement text. default value CENTER. background color widget. bitmap used display graphics widget. graphical image object. borderwidth represents size border. command This option procedure which must called every-time when state radiobutton changed. cursor mouse pointer changed specified cursor type. arrow, dot, etc. font represents font type widget text. normal foreground color widget text. height vertical dimension widget. specified number lines (not pixel). highlightcolor represents color focus highlight when widget focus. highlightbackground color focus highlight when widget having focus. image image object want display image radiobutton instead text. justify represents justification multi-line text. CENTER(default), LEFT, RIGHT. padx horizontal padding widget. pady vertical padding widget. relief type border. default value FLAT. selectcolor color radio button when selected. selectimage image displayed radiobutton when selected. state represents state radio button. default state Radiobutton NORMAL. However, this DISABLED make radiobutton unresponsive. text text displayed radiobutton. textvariable String type that represents text displayed widget. underline default value this option however, this option number character which underlined. value value each radiobutton assigned control variable when turned user. variable control variable which used keep track user's choices. shared among radiobuttons. width horizontal dimension widget. represented number characters. wraplength wrap text number lines setting this option desired number that each line contains only that number characters. Methods radiobutton widget provides following methods. Method Description deselect() used turn radiobutton. flash() used flash radiobutton between active normal colors times. invoke() used call procedure associated when state Radiobutton changed. select() used select radiobutton. Example from tkinter import selection(): selection "You selected option str(radio.get()) label.config(text selection) Tk() top.geometry("300x150") radio IntVar() Label(text "Favourite programming language:") lbl.pack() R1 Radiobutton(top, text="C", variable=radio, value=1, command=selection) R1.pack( anchor Radiobutton(top, text="C++", variable=radio, value=2, command=selection) R2.pack( anchor R3 Radiobutton(top, text="Java", variable=radio, value=3, command=selection) R3.pack( anchor label Label(top) label.pack() top.mainloop() Output: Next TopicPython Tkinter Scale prev next