next prev concatenate strings Python Python string collection Unicode characters. Python provides many built-in functions string manipulation. String concatenation process when string merged with another string. done following ways. Using operators Using join() method Using method Using format() function Let's understand following string concatenation methods. Using operator This easy combine strings. operator adds multiple strings together. Strings must assigned different variables because strings immutable. Let's understand following example. Example Python program show string concatenation Defining strings str1 "Hello str2 "Devansh" Operator used strings concatenation str3 str1 str2 print(str3) Printing combined string Output: Hello Devansh Explanation: above example, variable str1 stores string "Hello", variable str2 stores "Devansh". used operator combine these string variables stored str3. Using join() method join() method used join string which separator joined sequence elements. Let's understand following example. Example Python program string concatenation str1 "Hello" str2 "JavaTpoint" join() method used combine strings print("".join([str1, str2])) join() method used combine string with separator Space(" str3 ".join([str1, str2]) print(str3) Output: HelloJavaTpoint Hello JavaTpoint Explanation: above code, variable str1 stores string "Hello" variable str2 stores "JavaTpoint". join() method returns combined string that stored str1 str2. join() method takes only list argument. Using Operator operator used string formatting. also used string concatenation. Let's understand following example. Example Python program demonstrate string concatenation str1 "Hello" str2 "JavaTpoint" Operator used here combine string print("% (str1, str2)) Output: Hello JavaTpoint Explanation above code, represents string data-type. passed both variables's value that combined strings returned "Hello JavaTpoint". Using format() function Python provides str.format() function, which allows multiple substitutions value formatting. accepts positional arguments concatenates string through positional formatting. Let's understand following example. Example Python program show string concatenation str1 "Hello" str2 "JavaTpoint" format function used here concatenate string print("{} {}".format(str1, str2)) store result another variable str3 {}".format(str1, str2) print(str3) Output: Hello JavaTpoint Hello JavaTpoint Explanation: above code, format() function combines both strings stores into str3 variable. curly braces used position strings. Next TopicHow connect Database Python prev next