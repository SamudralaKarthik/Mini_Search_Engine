next prev combine dataframe Python Pandas Pandas Pandas inbuilt library Python which used work with relational data Python programming language. functions data structures which help operations relational data. data stored form rows columns two-dimensional data generally called dataframes pandas. have dataframes, then with help pandas, combine them merge them into single dataframe. Pandas provide logic combine data different dataframes well logic compare them. Using concat() function python, concatenate dataframes with help concat() function Pandas. concatenate data either row-wise column-wise. This function merges data axis (row column) performs logic another axis (another index). Example: import pandas from IPython.display import display First DataFrame dataFrame1 pd.DataFrame({'id': ['A1', 'A2', 'A3', 'A4'], 'Name': ['ABC', 'PQR', 'DEF', 'GHI'], 'Marks':[65,69,96,89]}) Second DataFrame dataFrame2 pd.DataFrame({'id': ['B1', 'B2', 'B3', 'B4'], 'Name': ['XYZ', 'TUV', 'MNO', 'JKL'], 'Marks':[56,96,69,98]}) frames [dataFrame1, dataFrame2] result pd.concat(frames) display(result) Output: Explanation: above code, have first imported Pandas library file. Then, created dataframes where each dataframe contains three columns four rows. Then used concat function, which concatenates these dataframes row-wise, with display function, printed this screen. Using joins pandas have understood concept joins database where join tables based some common attribute. same method applicable concatenation dataframes. simple concat() method, merged rows another created dataframe. join, define which type join want perform table, whether inner join outer join. Whatever type join either inner join (intersection) outer join (union), will defined join attribute. Example: import pandas from IPython.display import display dataFrame1 pd.DataFrame({'id': ['A1', 'A2', 'A3', 'A4'], 					'Name': ['ABC', 'PQR', 'TUV', 'JKL']}) dataFrame2 pd.DataFrame({'City': ['NOIDA', 'JAIPUR', 'MANALI', 'DELHI'], 					'Age': ['11', '10', '12', '17']}) default behaviour join='outer' inner join result pd.concat([dataFrame1, dataFrame2], axis=1, join='inner') display(result) Output: Explanation: above code, have daraframes which both contain columns four rows. Both dataframe have different column names, concat() function, have used inner join, which takes intersection part. axis attribute, have initialized value one, whole data. Example: import pandas from IPython.display import display dataFrame1 pd.DataFrame({'id': ['A1', 'A2', 'A3', 'A4'], 					'Name': ['ABC', 'PQR', 'TUV', 'JKL']}) dataFrame2 pd.DataFrame({'id': ['A1', 'A2', 'A3', 'A4'],'City': ['NOIDA', 'JAIPUR', 'MANALI', 'DELHI'], 					'Age': ['11', '10', '12', '17']}) default behaviour join='outer' inner join result pd.concat([dataFrame1, dataFrame2], axis=0, join='inner') display(result) Output: Since there common attribute inner join applied, then empty dataframe output. there common attribute both dataframes: Example: import pandas from IPython.display import display dataFrame1 pd.DataFrame({'id': ['A1', 'A2', 'A3', 'A4'], 					'Name': ['ABC', 'PQR', 'TUV', 'JKL']}) dataFrame2 pd.DataFrame({'id': ['B1', 'B2', 'B3', 'B4'], 'City': ['NOIDA', 'JAIPUR', 'MANALI', 'DELHI'], 'Age': ['11', '10', '12', '17']}) default behaviour join='outer' inner join result pd.concat([dataFrame1, dataFrame2], axis=0, join='inner') display(result) Output: Explanation: above code, have attribute, 'id', that common dataframe created based only common attributes. Using append() method Instead concat() method, append() method. This append() method applied dataframes. Example: import pandas from IPython.display import display First DataFrame dataFrame1 pd.DataFrame({'id': ['A1', 'A2', 'A3', 'A4'], 					'City': ['JAIPUR', 'MANALI', 'NOIDA', 'LUCKNOW']}) Second DataFrame dataFrame2 pd.DataFrame({'id': ['B1', 'B2', 'B3', 'B4'], 					'City': ['MUMBAI', 'UDAIPUR', 'RISHIKESH', 'KASHMIR']}) append method result dataFrame1.append(dataFrame2) display(result) Output: Explanation: above code, have merged dataframes using append method. Next TopicHow make Python auto clicker prev next