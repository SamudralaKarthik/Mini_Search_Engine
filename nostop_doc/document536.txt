next prev Python Program return Sign product Array this tutorial, will write Python program which returns Sign array's product. easy leetcode problem asked technical interviews. Let's understand problem statement. Problem Statement Example Input: nums [-1,-2,-3,-4, Output: Example Input: nums Output: Explanation Here, need calculate product list values; returns positive integer, sign will returns negative value, sign will else returns product above list 144, which positive integer; hence output Constraints need define separate function that returns following result. positive. negative. equal Let' write Python program solve this problem. Solution Let's understand below solution. class Solution: prodSign(self, list1): create variable store product prod iterate each element calculate product list1: prod prod call signfun() return sign based product value return self.signfun(prod) define function determine sign product signfun(self, num): num<0: return elif num>=1: return else: return Solution() nums [-1,-2,-3,-4,3,2,1] print(obj.prodSign(nums)) Output nums [-1, Output Explanation prodSign() function, defined prod variable store product variable. loop iterate each value updates prod value. first iteration, prod product will Now, assigned prod variable. second iteration, prod hence product will Once final product, call signfun() return Sign according result. Next TopicFabric Module Python prev next