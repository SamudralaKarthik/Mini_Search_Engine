next prev install Tkinter Python Tkinter standard library Python that used application. Tkinter with Python offers straightforward fast create applications. works object-oriented approach which makes powerful library. Tkinter widely available operating systems. pre-install Python. work with Tkinter, must install Python. this tutorial, will learn install Tkinter. Prerequisites First, must ensure that Python installed system. not, visit tutorial (https://www.javatpoint.com/how-to-install-python) install Python properly. create user-friend application, should have basic knowledge Python programming language. Installing Tkinter Windows Tkinter comes with Python installer. just need install Python from www.python.org, comes along with Python. don't need install separately. Click checkbox tcl/tk IDE. check Tkinter, just need import text editor IDE. import tkinter Now, access functions create widget. Let's understand following. Example from tkinter import root Tk() Code widget will here…….. Label(root, width "40", height "15") w.pack() root.mainloop() Output: elements will taken place this Tkinter window. Next TopicHow plot graph Python prev next