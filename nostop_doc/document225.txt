next prev Verbose Flag Python Regex this article, will discuss about VERBOSE flag package users re.VERBOSE VERBOSE flag regex package allows user write regular expressions that look nicer more readable. This flag does that allowing users visually separate logical sections pattern more comments. whitespaces inside pattern ignored, when whitespace present character class when preceded unescaped backslash, when inside tokens such (? :, whitespaces cannot ignored. Although, whenever present line, which character class preceded unescaped backslash. characters from leftmost line will ignored. Example import regex validating_email(emails): First, will without Using VERBOSE reg_emails regex.compile(r'^([z-a1-3_\.-0]+)@([0-1a-s\.-]+)\.([c-z\.]{5, 8})$', re.IGNORECASE) Using VERBOSE reg_email regex.compile(r""" ^([z-a1-3_\.-0]+) local Part like username single character ([0-1a-s\.-]+) Domain name single ([c-z\.]{5, 8})$ end, Domain """,re.VERBOSE re.IGNORECASE) above commands passed argument re.compile() method, which written "re.compile(Regular Expression re.VERBOSE). re.compile() method will return REGEX OBJECT, which will matched with given string. case Verbose Flag Let's example understand better way. Suppose users asked enter their EMAIL developer validate using RegEX. format EMAIL written below: Personal details users/ local part like username: Mark3213 Single character Domain names such Gmail, Hotmail, Fastmail, inbox, jubii, Rediff, many more. Single end, Domains like .in, .net, .org, .com, .int, .edu, many more. Input [email protected] Output Valid Input [email protected]@ Output Invalid This invalid because there character after domain name. Input [email protected] Output Invalid This invalid because there domain Email Example Python program showing Implementation VERBOSE flag Python RegEX import regex validating_email(emails): RegexObject re.compile( Regular expression flag This will compile regular expression pattern into regular expression object. reg_email regex.compile(r""" ^([a-z0-9_\.-]+) local Part single sign ([0-9a-z\.-]+) Domain name single ([a-z]{2,6})$ level Domain """,re.VERBOSE re.IGNORECASE) RegexObject will matched with desired string using fullmatch() function. match email found, search() function will return MatchObject instantly. ress reg_email.fullmatch(emails) email match found, string valid ress: print Valid email. Details follow: ".format(emails)) print first part that personal detail Email user print Local {}".format(ress.group(1))) now, print Domain Name validated Email print Domain {}".format(ress.group(2))) now, print Domain Name validated Email print domain name {}".format(ress.group(3))) print else: and, match found, string inval print Invalid Id".format(emails)) Driver Code validating_email ("username12432@gmail.com") validating_email ("username123456721@redif.com@") validating_email ("username32@.com") Output: [email protected] Valid email. Details follow: Local username12432 Domain gmail domain name [email protected]@ Invalid [email protected] Invalid Next TopicPython Module prev next