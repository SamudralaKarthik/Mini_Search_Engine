next prev Python Tkinter Scale Scale widget used implement graphical slider python application that user slide through range values shown slider select among them. control minimum maximum values along with resolution scale. provides alternative Entry widget when user forced select only value from given range values. syntax Scale widget given below. Syntax Scale(top, options) list possible options given below. Option Description activebackground background color widget when focus. background color widget. border size widget. default pixel. command procedure which called each time when move slider. slider moved rapidly, callback done when settles. cursor mouse pointer changed cursor type assigned this option. arrow, dot, etc. digits control variable used control scale data string type, this option used specify number digits when numeric scale converted string. font font type widget text. foreground color text. from_ used represent widget range. highlightbackground highlight color when widget doesn't have focus. highlighcolor highlight color when widget focus. label This some text which shown label with scale. shown left corner scale horizontal right corner scale vertical. length represents length widget. represents dimension scale horizontal dimension scale vertical. orient horizontal vertical depending upon type scale. relief represents type border. default FLAT. repeatdelay This option tells duration which button pressed before slider starts moving that direction repeatedly. default resolution smallest change which made scale value. showvalue value scale shown text form default. this option suppress label. sliderlength represents length slider window along length scale. default pixels. However, change appropriate value. state scale widget active default. this DISABLED make unresponsive. takefocus focus cycles through scale widgets default. this option don't want this happen. tickinterval scale values displayed multiple specified tick interval. default value tickinterval represents float integer value that specifies other range represented scale. troughcolor represents color through. variable represents control variable scale. width represents width through part widget. Methods Method Description get() used current value scale. set(value) used value scale. Example from tkinter import select(): sel "Value str(v.get()) label.config(text sel) Tk() top.geometry("200x100") DoubleVar() scale Scale( top, variable from_ orient HORIZONTAL) scale.pack(anchor=CENTER) Button(top, text="Value", command=select) btn.pack(anchor=CENTER) label Label(top) label.pack() top.mainloop() Output: Next TopicTkinter Scrollbar prev next