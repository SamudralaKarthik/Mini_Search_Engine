next prev Convert string JSON Python Before taking deep dive into topic, have glance what strings what JSON? Strings: sequence characters that denoted using inverted commas They immutable which means they cannot changed once declared. JSON: stands "JavaScript Object Notation", JSON files consist text that easily read humans present form attribute-value pairs. extension JSON files ".json" have look first approach converting string json Python. following program illustrates same. converting string json import json initialize json object i_string {'C_code': 'C++_code' 'Java_code' 'Python_code' printing initial json i_string json.dumps(i_string) print ("The declared dictionary i_string) print ("It's type type(i_string)) converting string json res_dictionary json.loads(i_string) printing final result print ("The resultant dictionary str(res_dictionary)) print ("The type resultant dictionary is", type(res_dictionary)) Output: declared dictionary {'C_code': 'C++_code' 'Java_code' 'Python_code' It's type <class 'str'> resultant dictionary {'C_code': 'C++_code' 'Java_code' 'Python_code' type resultant dictionary <class 'dict'> Explanation: It's time explanation that logic becomes clear- Since here objective convert string json file will first import json module. next step initialize json object which have subject name keys then their corresponding values specified. After this, have used dumps() convert Python object json string. Finally, will loads() parse JSON string convert into dictionary. Using eval() converting string json import json initialize json object i_string {'C_code': 'C++_code' 'Java_code' 'Python_code' printing initial json print ("The declared dictionary i_string) print ("Its type type(i_string)) converting string json res_dictionary eval(i_string) printing final result print ("The resultant dictionary str(res_dictionary)) print ("The type resultant dictionary type(res_dictionary)) Output: declared dictionary {'C_code': 'C++_code' 			'Java_code' 'Python_code' type <class 'str'> resultant dictionary {'C_code': 'C++_code': 'Java_code': 'Python_code': type resultant dictionary <class 'dict'> Explanation: understand what have done above program. Since here objective convert string json file will first import json module. next step initialize json object which have subject name keys then their corresponding values specified. After this, have used eval() convert Python string json. executing program, displays desired output. Fetching values Finally, last program will fetch values after conversion string json. Let's have look import json i_dict '{"C_code": "C++_code" "Java_code":17, "Python_code":28}' json.loads(i_dict) print(res["C_code"]) print(res["Java_code"]) Output: observe following things output- have converted string json using json.loads(). After this have used keys "C_code" "Java_code" fetch their corresponding values. Conclusion this Tutorial, learned convert string json using Python. Next TopicDBSCAN algorithm Python prev next