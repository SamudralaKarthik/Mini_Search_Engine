next prev Convert dataframe into list this tutorial, will learn convert dataframes into simple Python list. will learn about methods that convert dataframe into lists. Before proceeding with methods, let's have look what dataframe create dataframe Python using Panda. Pandas Dataframe dataframes panda module (two-dimensional) size module that potentially heterogeneous tabular data structure with axes (row columns), labelled with variables. simple words, data frame data structure which data aligned tabular form. Creating Dataframe Pandas create basic dataframe following program using Pandas module Python: Example Importing Panda module import pandas GivenData {'Famous Name':['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker' 'Age': [42, Creating DataFrame with DataFrame() function dataFrame jtp.DataFrame(GivenData) Print dataframe result print(dataFrame) Output: Famous Name Stark Iron Captain Rogers Hulk banner Spidy Parker that's create dataframe using Panda module after looking output, also figure dataframe looks like. Converting Dataframe into List will tolist() function from Panda module program following while converting given dataframe into list: dataframe.tolist() Let's this function example understand working tolist() function. Example: Importing Panda module import pandas GivenData {'Famous Name':['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker' 'Age': [42, Creating DataFrame with DataFrame() function dataFrame jtp.DataFrame(GivenData) Converting dataframe into list List dataFrame.values.tolist() Print converted list data result print(List) Output: [['Stark Iron', 42], ['Captain Rogers', 95], ['Hulk banner', 38], ['Spidy Parker', 18]] Methods Convert Dataframe into List: dataframe converted into Python list many ways. this section, will discuss methods that going convert given dataframe into list. Here, will following four methods with help tolist() function: Converting dataframe while containing rows Converting dataframe into nested list Converting dataframe into nested list columns Converting dataframe into list with column names included Now, let's learn about each method with example understand them better way. Method Converting dataframe while containing rows this method, will convert given dataframe into list that will contain rows particular column from dataframe. Look following program understand implementation this method: Example Importing Panda module import pandas GivenData {'Famous Name':['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker' 'Age': [42, Creating DataFrame with DataFrame() function dataFrame jtp.DataFrame(GivenData) Converting dataframe Famous Names column into single list FamousNames dataFrame['Famous Name'].tolist() Printing converted list data print(FamousNames) Output: ['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker'] output that have converted famous name column defined dataframe into single list printed output. Method Converting dataframe into nested list this method, will convert given dataframe into nested list that will contain rows columns from dataframe separately. will this method following example understand implementation: Example Importing Pandas module import pandas GivenData {'Famous Name':['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker' 'Age': [42, Creating DataFrame with DataFrame() function dataFrame jtp.DataFrame(GivenData) Initializing empty list ResultList Using loop iterating through dataframe columns column dataFrame.columns: listing dataFrame[column].tolist() ResultList.append(listing) appending dataframe columns into list Print converted list data print(ResultList) Output: [['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker'], [42, 18]] Method Converting dataframe into nested list columns Under this method, will converting given dataframe into list that will contain multiple lists that having columns row. Look following program understand implementation this method: Example Importing Panda module import pandas GivenData {'Famous Name':['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker' 'Age': [42, Creating DataFrame with DataFrame() function dataFrame jtp.DataFrame(GivenData) Converting dataframe into list List dataFrame.values.tolist() Print converted list data result print(List) Output: [['Stark Iron', 42], ['Captain Rogers', 95], ['Hulk banner', 38], ['Spidy Parker', 18]] That's this method convert given dataframe into list containing multiple lists having data from columns rows. Method Converting dataframe into list with column names included this method when want convert given dataframe into list that contains multiple lists having columns with column names dataframe along with rows. will this method following example understand implementation: Example Importing Panda module import pandas GivenData {'Famous Name':['Stark Iron', 'Captain Rogers', 'Hulk banner', 'Spidy Parker' 'Age': [42, Creating DataFrame with DataFrame() function dataFrame jtp.DataFrame(GivenData) Converting dataframe into multiple lists listing [dataFrame.columns.values.tolist()] dataFrame.values.tolist() Printing converted list data print(listing) Output: [['Famous Name', 'Age'], ['Stark Iron', 42], ['Captain Rogers', 95], ['Hulk banner', 38], ['Spidy Parker', 18]] ************* Next TopicANOVA Test Python prev next