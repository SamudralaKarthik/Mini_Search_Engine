next prev Python Program accepting strings which contains vowels this tutorial, will discuss Python program accept only string which contains every vowel. following example, pass string check whether vowels present not. have considered that both upper case lowercase vowels acceptable. That "A", "E", "I", "O", "U" "a", "e", "i", "o", "u". Example: Input string1: AEIaeiytstwI Output: Accepted Reason: vowels except 'u' present Input string1 Input string2: AbCdEfHiMnOtUv Output: Accepted Reason: vowels present. Solution Approach first, will create vowels using set() function. Then will check whether each character string vowel not. character vowel, will into "string_1". After coming loop, will check length "string_1". string will only accepted length "string_1" equal length "vowel_1" string; otherwise, will accept string. Method check(string): string string.lower() set() function will used converting "aeiou" string into #characters vowel_1 set("aeiou") set() function will convert empty dictionary into empty string_1 set({}) will loop through each character string character_1 string: Now, will check character present inside "vowel_1" #not. characters present, then will into "string_1" #using "add" method character_1 vowel_1 string_1.add(character_1) else: pass Then, will check length "string_1" equal length "vowel_1" not. len(string_1) len(vowel_1) print ("The string Accepted") else print ("The string Accepted") Driver code __name__ "__main__" string (input ("Enter String characters: Calling function check(string) Output: Enter String characters: aideiofguhs string Accepted Enter String characters: aideiofghs string Accepted Explanation: above program will check inserted string characters, character matches vowel character, will string_1 set, character vowel, will pass next. Then have condition that length string_1 equal length vowel_1, string will accepted. length string_1 equal length vowel_1, will accept string. Method check(string): string string.replace(' string string.lower() vowel_1 [string.count('a'), string.count('e'), string.count( 'i'), string.count('o'), string.count('u')] present vowel count array vowel_1.count(0) return('The string Accepted') else: return('The string Accepted') Driver code __name__ "__main__": string (input ("Enter String characters: print (check(string)) Output: Enter String characters: "aiD" 'OeiUo' "FGu" string Accepted Enter String characters: 'aidE' 'eiIOo' "fgHYK" 'hs' string Accepted Explanation: above code, have condition that present vowel count array, string will accepted. present string contains vowels, string will accepted. Method check(string): (set (string.lower()).intersection("aeiou")) return ("The string Accepted") else: return ("The string Accepted") Driver code __name__ "__main__": string (input ("Enter String characters: print (check(string)) Output: Enter String characters: gIrwi aTheDy uKmtR wmsof string Accepted Enter String characters: aidc Eie iIOof gH YKhs string Accepted Explanation: above code, have condition that string's length greater equal length vowel_1, that contains vowels. Then will accept string; otherwise, will accept string. Conclusion this tutorial, have shown write Python program accepting string which contains vowel characters. users also same code different types conditions particular characters. have explained different methods same. Next TopicBasic Python programs prev next