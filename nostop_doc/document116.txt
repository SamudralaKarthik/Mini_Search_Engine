next prev print pattern Python Python, loop used print various patterns. Printing various patterns most common asked programming questions interview. multiple loops used print patterns where first outer loop used print number rows, inner loop used print number columns. Most patterns following concepts. outer loop print number rows. inner loops print number columns. variable print whitespace according required place Python. this tutorial, will discuss common patterns. Print Pyramid, Star, diamond pattern Python this section, will learn common pyramid patterns. Pattern Simple pyramid pattern Example This example print simple pyramid pattern int(input("Enter number rows")) outer loop handle number rows range(0, inner loop handle number columns values changing according outer loop range(0, printing stars print("* end="") ending line after each print() Output: Explanation: above code, have initialized variable enter number rows pattern. entered range outer loop will iteration inner loop depends outer loop. inner loop responsible print number columns. first iteration, value increased becomes 0+1, inner loop iterated first time print star(*). second iteration, value increased becomes 1+1, inner loop iterated times print two-star (*). argument prevents jump into another line. will printer star until loop valid. last print statement responsible ending line after each row. Pattern Reverse right angle pyramid Example This example print simple reversed right angle pyramid pattern rows int(input("Enter number rows:")) rows used number spaces range(0, rows): range(0, print(end=" decrement value after each iteration range(0, print("* end="") printing star print("") Output: Pattern Printing Downward half Pyramid Code rows int(input("Enter number rows: outer loop executing reversed order range(rows -1): range(0, print("*", end=' print(" Output: Enter number rows: Pattern Printing Triangle Pyramid Code int(input("Enter number rows: range(0, range(0, print(end=" decrementing after each loop range(0, printing full Triangle pyramid using stars print("* end=' print(" Output: Enter number rows: Pattern Downward Triangle Pattern Code rows int(input("Enter number rows: used print space rows Outer loop reverse order range(rows, -1): Inner loop will print number space. range(k, -1): print(end=" This inner loop will print number stars range(0, print("*", end=" print("") Output: Pattern Diamond Shaped Pattern Code rows int(input("Enter number rows: used print space rows Outer loop print number rows range(0, rows): Inner loop used print number space range(0, print(end=" Decrement after each iteration This inner loop used print stars range(0, print("* end="") print("") Downward triangle Pyramid used print space rows Output downward triangle pyramid range(rows, -1): inner loop will print spaces range(k, -1): print(end=" Increment after each iteration This inner loop will print number stars range(0, print("* end="") print("") Output: Enter number rows: Pattern Print pyramid single pattern Code rows input("Enter number rows: Outer loop will print number rows range(0, rows): This inner loop will print stars range(0, print("*", end=' Change line after each iteration print(" second pattern range(rows -1): range(0, print("*", end=' print(" Output: Enter number rows: Pattern Hourglass Pattern Code rows int(input("Enter number rows: rows This used print downward pyramid range(rows, -1): range(k -1): print(end=" range(0, i+1): print("* end="") print() This used print upward pyramid rows range(0 rows+1): range(0 print(end="") range(0, print("* end="") print() Output: Enter number rows: have discussed basic pyramid pattern using loops. concept pattern depends logic proper loop. Number Pattern Python this section, describe programs different types number patterns. Let's understand following patterns one. Pattern Number Pattern Code rows int(input("Enter number rows: Outer loop will print number rows range(rows+1): Inner loop will print value after each iteration range(i): print(i, end=" print number line after each display pattern correctly print(" Output: Enter number rows: Explanation above code, have printed numbers according rows value. first prints single number. Next, numbers, prints second row, next three number prints third Pattern Half pyramid pattern with number Code rows int(input("Enter number rows: This will print rows range(1, rows+1): This will print number column range(1, print(j, end=' print("") Output: Enter number rows: above code, have printed column value inner loop. Pattern Inverted Pyramid Pattern Code rows int(input("Enter number rows: 5")) Reversed loop downward inverted pattern range(rows, -1): Increment after each iteration range(1, print(k, end=' print() Output: Enter number rows: Explanation: above code, have used reversed loop print downward inverted pyramid where number reduced after each iteration. Pattern Same number Inverted Pyramid Code rows int(input("Enter number rows: rows value assign variable rows Download reversed loop range(rows, -1): range(0, this will print same number print(n, end=' print() Output: Enter number rows: Pattern Descending Order Number Code rows int(input("Enter number rows: Downward loop inverse loop range(rows, -1): assign value after each iteration range(0, print reduced value each line print(n, end=' print("\r") Output: Enter number rows: Pattern Print numbers Pattern Code current_Number stop rows Number rows print numbers range(rows): range(1, stop): print(current_Number, end=' current_Number print("") stop Output: Pattern Reverse Pattern from Code rows int(input("Enter number rows: range(0, rows inner loop decrement values range(rows -1): print(j, end=' print() Output: Enter number rows: Pattern Print Numbers Code rows int(input("Enter number rows: outer file loop print number rows while rows: Check column after each iteration while print values print((i end=" print() Output: Enter number rows: Pattern Square Pattern using with number Code rows int(input("Enter number rows: range(1, rows range(1, rows Check condition value smaller equal than then print otherwise print print(i, end=' else: print(j, end=' print() Output: Enter number rows: Pattern Multiplication Number Column rows int(input("Enter number rows: range(1, rows): range(1, prints multiplication column print(i end=' print() Output: Enter number rows: above section, have discussed basic number patterns. will make strong command Python loop. type pattern using loop. Alphabets Letters Pattern Python know that the, each alphabet ASCII value, define character print screen. Let's understand these patterns following examples Pattern Right-angled pattern with characters Code print("The character pattern asciiValue #ASCII value range(0, range(0, will convert ASCII value character alphabate chr(asciiValue) print(alphabate, end=' asciiValue print() Output: character pattern Explanation above code, have assigned integer value asciiValue variable which ASCII value defined loop print five rows. inner loop body, converted ASCII value into character using char() function. will print alphabets, increased asciiValue after each iteration. Pattern Right-angled Pattern with Same Character Code print("The character pattern asciiValue int(input("Enter ASCII value print pattern: User define value (asciiValue asciiValue <=122): range(0, range(0, will convert ASCII value character alphabate chr(asciiValue) print(alphabate, end=' print() else: print("Enter valid character value") Output: character pattern Enter ASCII value print pattern: Pattern Display letter word Pattern Code str1 "JavaTpoint" str1: print(x) Output: Jav Java JavaT JavaTp JavaTpo JavaTpoi JavaTpoin JavaTpoint word print characters. Pattern Equilateral triangle pattern with characters Code print("Print equilateral triangle Pyramid with characters asciiValue s) range(0, range(0, print(end=" Decreased value after each iteration range(0, alphabate chr(asciiValue) print(alphabate, end=' Increase ASCII number after each iteration asciiValue print() Output: Print equilateral triangle Pyramid with characters this article, have discussed basic pattern programs. These patterns commonly asked during interview also helpful understand concept Python loop. Once logic program, print pattern using Python loops. Next TopicHow remove element from list Python prev next