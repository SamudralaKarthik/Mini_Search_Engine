next prev Python Program Rotate Image this tutorial, will write Python program solve rotation image (matrix). problem related matrix. Let's understand problem statement. Problem Statement nxn matrix represents image. need rotate image degrees clockwise. need perform this operation using in-place means need another matrix rotation. After rotation, image will look below. Example Example Input matrix [[1,2,3],[4,5,6],[7,8,9]] Output: [[7,4,1],[8,5,2],[9,6,3]] Example Input: matrix [[5, 11], 10], [13, [15, 16]] Output: [[15, [14, [12, [16, 11]] Example Input: matrix [[1]] Output: [[1]] should follow following constraints matrix.length matrix[i].length -1000 matrix[i][j] 1000 Solution will implement following solution. First, number rows columns matrix. Iterate inner loop outer loop transpose matrix. Reverse transposed matrix. Let's following code. Example rotate(matrix): :type matrix: List[List[int]] :rtype: None return anything, modify matrix in-place instead. if(matrix==None len(matrix) len(matrix[0]) 0):return range(len(matrix)): range(row): matrix[row][col], matrix[col][row] matrix[col][row], matrix[row][col] i,row enumerate(matrix): matrix[i]=row[::-1] return matrix [[1,2,3],[4,5,6],[7,8,9]] print('The input matrix is:', mat) print("The matrix after rotation: rotate(mat)) Output: input matrix [[1, matrix after rotation: [[7, Explanation above code, have created method that takes matrix rotated. iterated outer loop, which will iterate each matrix, outer loop iterated over column each row. changed matrix's rows columns columns rows inside inner loop. print transposed matrix, will look below. range(len(matrix)): range(row): matrix[row][col], matrix[col][row] matrix[col][row], matrix[row][col] print(matrix) will give following output [[1, look closely transposed matrix, just step away from getting desired output. only need reverse each row. Hence reversed each matrix with help enumerate() function. i,row enumerate(matrix): matrix[i]=row[::-1] print(i, row) will give following output next line, revered rows returned matrix. Let's understand another solution. Solution this solution, will Switch rows down, example there rows: switch row, (n-1) row, Then find transpose resultant matrix. Let's understand following example. Example rotate(matrix): if(matrix==None len(matrix) len(matrix[0]) 0):return rows len(matrix) cols len(matrix[0]) first,last 0,rows-1 while(first<last): matrix[first],matrix[last] matrix[last],matrix[first] first +=1 last -=1 range(0,rows): range(i+1,cols): matrix[i][j],matrix[j][i] matrix[j][i],matrix[i][j] return matrix [[1,2,3],[4,5,6],[7,8,9]] print('The input matrix is:', mat) print("The matrix after rotation: rotate(mat)) Output: input matrix [[1, [[7, matrix after rotation: [[7, Conclusion this tutorial, have solved interesting Python matrix problems that asked many organizations. have implemented solution using multiple codes with explanation. solution approach, your own. Next TopicValidate Address Python prev next