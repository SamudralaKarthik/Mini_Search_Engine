next prev Python Tkinter-Top level Widget Tkinter Python module designed develop project from website. stands Graphical User Interface. Tkinter most widely used modules create faster. functionality module makes work designer easier. Whenever install software pc, dialog appears screen asking permissions software. window stops entire background work until receives input, appearing everything console. This example top-level widget. This tutorial explains create top-level widget using Python Tkinter module. Top-level widget acts like window managed window manager. generally used more than dialog extra project information. Function used: Toplevel() Syntax: Toplevel(root, fg, bd, height, width, font, cursor…) root: root window(the first window screen which other windows opened)(optional parameter). Background color widget Example: from tkinter import Toplevel(bg 'yellow') obj.mainloop() Output: fg: foreground color widget bd: width widget's border. default value taken height, width: Dimensions widget Example: from tkinter import Toplevel(bg 'yellow', height width obj.mainloop() Output: font: font type used text window cursor: type cursor appears when take mouse pointer into window. names different types cursors predefined. Example: from tkinter import Toplevel(bg 'yellow', height width cursor 'hand1') obj.mainloop() Output: There still more optional parameters specifying several other options window appearance. Methods module top-level widget: mainloop(): used event loop. call this method when opening window, which blocks every other code from execution until window closed. Using only this method, create simple top-level window: import tkinter t.Toplevel() obj.mainloop() Output: iconify(): removes window from screen minimizes into taskbar icon. import tkinter t.Tk() obj.iconify() obj.mainloop() Output: deiconify(): converts converted icon back window windraw(): removes window from console. won't destroy window hides undo method, deiconify() iconify() used. title(): give title window state(): Returns state created window Iconic, Withdrawn, etc. frame(): creates rectangular region arrangement padding widgets inside window. relief(): Sets certain simulated effects window/ widget. These options predefined. create root window top-level window Code: import tkinter root_window t.Tk() label t.Label(root_window, text "Root window") root_window.geometry("400x400") root_window.title("Top") top_window t.Toplevel() label2 t.Label(top_window, text "Top-level window") top_window.geometry("400x400") top_window.title("Top") label.grid() label2.grid() top_window.mainloop() Output: Tkinter also allows multiple top-level windows root window. Here example program that executed root window with top-level windows when user clicks buttons: from tkinter import root Tk() root.title("Root window") label Label(root, text "This root window") root.geometry("600x600"); toplvl2(): top2 Toplevel() top2.title("Second level window") top2.geometry("400x400") label2 Label(top2, text "This second level window") button2 Button(top2, text "CLOSE", command top2.destroy) label2.pack() button2.pack() top2.mainloop() toplvl1(): top1 Toplevel(root) top1.title("First level window") top1.geometry("500x500") label1 Label(top1, text "This first level window") button1 Button(top1, text "EXIT", command top1.destroy) button2 Button(top1, text "Open another window", command toplvl2) label1.pack() button2.pack() button1.pack() top1.mainloop() button Button(root, text "open window", command toplvl1) label.pack() button.place(x 250, 250) root.mainloop() Output: With knowledge gained till now, create simple project called "Secret message". We'll implement program that opens top-level window with secret labeled message button click: from tkinter import root Tk() root.title("Secret message") root.geometry("400x400") root.config(bg "black") top_level(): Toplevel() top.title("Here comes") label Label(top, text "Congratulations result, we're proud you") label.pack() top.mainloop() button Button(root, text "Click", command top_level) button.place(x 600, 600) button.pack() root.mainloop() Output: simple. Isn't Tkinter application with many features, level widget primary ones. Next TopicRemove First Character from String Python prev next