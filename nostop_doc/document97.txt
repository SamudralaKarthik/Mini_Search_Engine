next prev install OpenCV Python OpenCV open-source Python library, which used understand content digital image. abbreviation form computer vision. extracts description from real-time image digital image, which object, text description, perform many tasks using OpenCV library such face detection, face recognition, blob detection, edge-detection, image filter, template matching, etc. work with OpenCV, need install Python environment. Installation OpenCV Here, install OpenCV using following ways. Using command Using Anaconda Using command Open command prompt type following command. install opencv-contrib-python --upgrade install without extra module following command. install opencv-python Now, will verify proper installation OpenCV. Import module print version. properly installed, then will show version. Using Anaconda Anaconda software package Python. Anaconda with Jupyter best work with OpenCV. First, need install Anaconda graphics installer from official site. Now, choose suitable installer. Once download completed. Open Anaconda prompt type following command. conda install conda-forge opencv Then, enter button will download rated OpenCV configuration. Now, ready work with OpenCV. learn complete OpenCV through OpenCV tutorial (https://www.javatpoint.com/opencv). Next TopicHow print same line Python prev next