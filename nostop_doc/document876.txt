next prev Python Tkinter Scrollbar scrollbar widget used scroll down content other widgets like listbox, text, canvas. However, also create horizontal scrollbars Entry widget. syntax Scrollbar widget given below. Syntax Scrollbar(top, options) list possible options given below. Option Description activebackground background color widget when focus. background color widget. border width widget. command procedure associated with list which called each time when scrollbar moved. cursor mouse pointer changed cursor type this option which arrow, dot, etc. elementborderwidth represents border width around arrow heads slider. default value Highlightbackground focus highlighcolor when widget doesn't have focus. highlighcolor focus highlighcolor when widget focus. highlightthickness represents thickness focus highlight. jump used control behavior scroll jump. then callback called when user releases mouse button. orient HORIZONTAL VERTICAL depending upon orientation scrollbar. repeatdelay This option tells duration which button pressed before slider starts moving that direction repeatedly. default repeatinterval default value repeat interval 100. takefocus focus through this widget default. this option don't want this behavior. troughcolor represents color trough. width represents width scrollbar. Methods widget provides following methods. Method Description get() returns numbers which represents current position scrollbar. set(first, last) used connect scrollbar other widget w. yscrollcommand xscrollcommand other widget this method. Example from tkinter import Tk() sb Scrollbar(top) sb.pack(side RIGHT, fill mylist Listbox(top, yscrollcommand sb.set line range(30): mylist.insert(END, "Number str(line)) mylist.pack( side LEFT sb.config( command mylist.yview mainloop() Output: Next TopicTkinter Text prev next