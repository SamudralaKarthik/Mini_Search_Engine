next prev Write Python Program Print Possible Combination Integers this tutorial, will write Python program that prints possible combinations list elements. will take three distinct integers input print possible combinations from digits. quite easy program; interview entry-level job. Problem Solving Approach First, accepts three unique integer values. Then, list using append() method. Now, three loops print integer number list none their indexes equal each other. Terminate loop. Let's following example. Python Program Program comb(L): a=int(input("Enter first number:")) b=int(input("Enter second number:")) c=int(input("Enter third number:")) L.append(a) L.append(b) L.append(c) range(3): 	for range(3): 		for range(3): 			# check indexes 			# same 			if (i!=j j!=k i!=k 				print(L[i], L[j], L[k]) 				 comb([]) Output: Enter first number: Enter second number: Enter third number: Explanation above code, take three unique integer numbers append list. loop iterated which shows basically indexes three elements list. used conditions statement, none indexes equal indexes equal element associated with particular element list printed. below example, will only print combination distinct elements. Let's understand following example. Example Print combination unique elements. comb(L): a=int(input("Enter first number:")) b=int(input("Enter second number:")) c=int(input("Enter third number:")) L.append(a) L.append(b) L.append(c) range(3): 	for range(3): 		for range(3): 			# check indexes 			# same 			if (i!=j j!=k i!=k): 				print(L[i], L[j], L[k]) 				 comb([]) Output Enter first number: Enter second number: Enter third number: Output Enter first number: Enter second number: Enter third number: Next TopicModulo String Formatting Python prev next