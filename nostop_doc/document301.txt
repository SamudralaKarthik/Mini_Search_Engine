next prev Amicable Numbers Python this tutorial, will learn amicable numbers Python. First, will understand what amicable numbers they used. Amicable numbers different numbers related that proper divisor each equal other number. other words, proper divisor proper divisor should equal opposite number. sum_x sum_y Suppose have numbers, 426, need find proper divisors 426; then proper divisor same with 426. proper divisor should equal 426, proper divisor 426 should equal 123. check numbers amicable Python Following steps find whether numbers amicable not. First, take integer number input from user. proper divisor both numbers them. check whether given number equal opposite numbers. they equal then they Amicable else not. output. Program x=int(input('Enter first number y=int(input('Enter second number sum1=0 sum2=0 range(1,x): x%i==0: sum1+=i range(1,y): y%j==0: sum2+=j if(sum1==y sum2==x): print('Given numbers Amicable!') else: print('Given numbers Amicable!') Output: Enter first number 220 Enter second number Given numbers Amicable! Let's another output. Enter first number Enter second number 456 Given numbers Amicable! Explanation above example, take user input store separate variables. used loop statement find proper divisor both numbers. Then find proper divisors both numbers. Using statement, checked proper divisor numbers equals other number vice-versa. count pair amicable pairs from list have lists integer number, where will pick each number from both lists compare them. numbers amicable, increment count return. Let's understand following example. Example Python3 program count amicable pairs array Calculate proper divisors sumOfDiv(x): range(2, return Check pair amicable CheckAmicable(a, sumOfDiv(a) sumOfDiv(b) return True else: return False countPairs(arr, count range(0, range(i CheckAmicable(arr[i], arr[j]): count count return count list1 [220, 284, 1184, 1210, len(list1) print(countPairs(list1, n1)) list2 [2620, 2924, 5020, 5564, 6232, 6368] len(list2) print(countPairs(list2, n2)) Output: This simple count amicable number pair. traverse each pair check they form amicable pair. Next TopicContext Manager Python prev next