next prev Change "legend" Position Matplotlib this tutorial, will discuss change legend position Matplotlib. First, will discuss some basic concepts: Matplotlib powerful visualization library written Python plots arrays. developed hands John Hunter within year 2002. Matplotlib could data visualization tool multi-platform platforms based NumPy arrays developed integrate alongside wider SciPy stack. legend space that describes various elements that make graph. Within matplotlib library, there function named legend() that utilized create axes-specific legend. This attribute legend() uses attribute define exact location legend.Default value will "best" (upper left). strings that best upper left, upper right, lower left, lower left right middle left middle, lower center, center will place legend appropriate area axes/figure. Location String Location String Best Upper right Upper left Lower left Lower right Right Centre left Centre right Lower centre Upper centre centre Approach Step will import required modules libraries. Step will import create data. Step will Plot chart Step will legend Step will position legend using loc. Example First, will import required module import numpy import matplotlib.pyplot mplot Now, will create data npy.linspace(1, npy.random.seed(1) npy.random.randint(0, Then, will plot graph mplot.plot(x1, Here, will legend mplot.legend(['Legend']) mplot.show() Output: Example With Position Upper Right First, will import required module import numpy import matplotlib.pyplot mplot Now, will create data npy.linspace(1, npy.random.seed(1) npy.random.randint(0, then, will plot graph mplot.plot(x1, Here, will legend position upper right mplot.legend(['Legend'], 'upper right') mplot.show() Output: Example Position legend Lower Left, with also First, will import required module import numpy import matplotlib.pyplot mplot Now, will create data npy.linspace(1, npy.random.seed(1) npy.random.randint(0, then, will plot graph mplot.plot(x1, Here, will legend position lower left That mplot.legend(['Legend'], mplot.show() Output: Legend lower left Example Legend Position Center First, will import required module import numpy import matplotlib.pyplot mplot Now, will create data npy.linspace(1, npy.random.seed(1) npy.random.randint(0, then, will plot graph mplot.plot(x1, Here, will legend position upper left mplot.legend(['Legend'], 'center') mplot.show() Output: Conclusion this tutorial, showed change position legend using Matplotlib Python. Next TopicHow Check Element Exists List Python prev next