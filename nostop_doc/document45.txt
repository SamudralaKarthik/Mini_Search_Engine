next prev Python module python module provides functions variables which used manipulate different parts Python Runtime Environment. lets access system-specific parameters functions. import First, have import module program before running functions. sys.modules This function provides name existing python modules which have been imported. sys.argv This function returns list command line arguments passed Python script. name script always item index rest arguments stored subsequent indices. sys.base_exec_prefix This function provides efficient same value exec_prefix. running virtual environment, value will remain same. sys.base_prefix during Python startup, before site.py run, same value prefix. sys.byteorder indication native byteorder that provides efficient something. sys.maxsize This function returns largest integer variable. sys.path This function shows PYTHONPATH current system. environment variable that search path python modules. sys.stdin object that contains original values stdin start program used during finalization. restore files. sys.getrefcount This function returns reference count object. sys.exit This function used exit from either Python console command prompt, also used exit from program case exception. executable value this function absolute path Python interpreter. useful knowing where python installed someone else machine. sys.platform This value this function used identify platform which working. Next TopicPython Tutorial prev next