next prev Iterate Dictionary Python Foundation/ brief pre-requisite knowledge: dictionary data types available Python. aware sets lists, dictionary another data storage simply. formal definition, dictionary unordered collection data stored form values attached their respective keys. what these keys values? dictionary similar index. represents value holds. Hence, inside dictionary, will store data value pairs: When open textbook search topic, search topic index page page number. {key1: value1, key2: value2, key3: value3….keyn: valuen} actual data need values, keys give identification number values. This article discusses iterate over values dictionary. ways using which achieve this. We'll discuss with examples each method: Using inbuilt Methods Code: dictionary 'A', 'B', 'C'} print ("Keys dictionary: dictionary. keys print ("Values dictionary: dictionary. values print ("The data stored dictionary: dictionary. items Output: Methods used code: Dictionary.keys() Returns list keys dictionary Dictionary.values() Returns list values dictionary Dictionary.items() Returns list key-value pairs tuples dictionary Understanding: just used methods already defined Python manipulate dictionary code. first statement, dictionary created. next three statements, used .keys(), .values() .items() prints keys, values (key: value) pairs dictionary. Using basic Functionality Dictionary: Code: dictionary 'A', 'B', 'C'} print ("The dictionary dictionary) print ("Keys dictionary: dictionary: print print ("Values dictionary: dictionary: print (dictionary [i]) Output: Understanding: There important points grasped from above code: iterate variable over dictionary Python, dictionary's keys will traversed. Now, want values keys hold, need dictionary which use- dictionary [key]. These things we've done code. First, declared dictionary printed Then, traversed through dictionary's keys using variable 'i'. next loop, traversed through keys printed their values: same loop printing both keys values: dictionary: print print (dictionary [i], print Output: Iterating items We've already seen method items used print (key: value) pairs dictionary list tuples. more clear representation every key-value pair dictionary, iterate variable method: Code: dictionary 'A', 'B', 'C'} print ("The dictionary dictionary) dictionary. items print Output: Program using types Iteration Methods: dictionary 'Hydrogen', 'Helium', 'Lithium'} print ("\nThe dictionary: dictionary) print ("\n") print ("Without using inbuilt functions: print ("Keys values dictionary: dictionary: print ('key: print ('value: dictionary [i], print print ("\nUsing inbuilt functions: print ("Keys dictionary: dictionary. keys print ("Values dictionary: dictionary. values print ("The data stored dictionary: dictionary. items print ("\nTuple-wise representation: dictionary. items print Output: Summary: this Tutorial, discussed: Given dictionary, iterate over keys? Given dictionary, iterate over values? Iterating over dictionary with inbuilt Python methods without inbuilt methods. Given key, find value holding? Tuple arrangement key-value pairs dictionary using .items Next TopicHow Iterate through List Python prev next