next prev Multiply Elements list Python this tutorial, will learn multiply elements list Python. have look some examples understand objective- Input Output observe that output have obtained product elements present list. Input 'a'] Output aaa Since first element three, printed three times output. will learn following methods- Traversing list Using NumPy Using lambda Let's start with first one, Traversing list Consider program given below- #creating function multiply_ele(list_value1): #multiply elements prod=1 list_value1: prod prod*i return prod #initializing list list_value1 [10, list_value2 #displaying resultant values print("The multiplication elements list_value1 multiply_ele(list_value1)) print("The multiplication elements list_value2 multiply_ele(list_value2)) Output: multiplication elements list_value1 240240 multiplication elements list_value2 5040 Explanation- It's time have look explanation above program- first step, have created function that will make list input. function definition, have used loop that takes each element from list, multiplies with initially, then prints resultant value product. next step, have initialized lists then passed them into function. executing this program, desired output displayed. second program, will NumPy help implement same. Using NumPy following program illustrates done Python. #importing NumPy module import numpy #initializing list list_value1 [10, list_value2 #using numpy.prod() res_list1 numpy.prod(list_value1) res_list2 numpy.prod(list_value2) #displaying resultant values print("The multiplication elements list_value1 res_list1) print("The multiplication elements list_value2 res_list2) Output: multiplication elements list_value1 240240 multiplication elements list_value2 5040 Explanation- Let's understand what have done above program. first step, have imported NumPy module. next step, have initialized values lists, list_value1 list_value2. After this, will prod() that will compute product elements present lists. executing program, expected output displayed. Finally, will learn lambda used multiply elements list. Using lambda program given below demonstrates same- #importing module from functools import reduce #initializing list list_value1 [10, list_value2 #using numpy.prod() res_list1 reduce((lambda b:a*b), list_value1) res_list2 reduce((lambda b:a*b), list_value2) #displaying resultant values print("The multiplication elements list_value1 res_list1) print("The multiplication elements list_value2 res_list2) Output: multiplication elements list_value1 240240 multiplication elements list_value2 5040 Explanation- understand what happened above program. first step, have imported reduce from After this, have initialized lists, list_value1 list_value2. have used precise defining functions, which lambda, then provided required functionality. executing program, desired values displayed. Conclusion this tutorial, learned various approaches multiplying elements present list Python. Next TopicDifference between module function Python prev next