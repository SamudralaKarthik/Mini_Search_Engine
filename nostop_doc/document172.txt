next prev convert Bytes string Python this tutorial, will learn convert Bytes String using Python script. will also explore these data types efficiently. include Python approaches this tutorial because Python longer use. Before digging deep into this topic, first, need understand basic introduction Byte data type. Byte Data Type Python familiar with Python, then he/she must already know about byte data type. someone friendly with Python, then will explain this concept. Let's understand following example. Example string1 'Welcome JavaTpoint' print(type(string1)) string2 b'Welcome JavaTpoint' print(type(string2)) Output: <class 'str'> <class 'bytes'> Explanation define strings with same value. Both values looking similar, data types same. first string1 variable string data type, other byte data type. second string2 variable prefixed with 'b,' which means produces byte data type instead data type. difference between these data types string sequence Unicode characters (encoded UTF -16 UTF-32 entirely depends Python's compilation). Bytes byte represents integer between 255, denote 'B.' convert string bytes writing 'be literal front regular string. Note Python ignores prefix 'B.' Difference between String Bytes Both bytes data types used Byte type objects Python 2.x, true case Python 3.x. critical difference between bytes string that strings easy read human-readable where byte eventually machine-readable, string also converted into byte before processing. When declare byte data type Python, directly stored disk string converted into byte then stored disk. Strings used signify characters, words, sentences, whereas Bytes represent low-level binary data structures. Convert Bytes String Python Using decode() method Python provides built-in decode() method, which used convert bytes string. Let's understand following example. Example byteData b"Lets \xf0\x9f\x8d\x95!" Let's check type print(type(byteData)) str1 byteData.decode('UTF-8') print(type(str1)) print(str1) Output: <class 'bytes'> <class 'str'> Lets 🍕! Explanation passed encoding format above code, decoded bytes object into string, printed Using Bytes String with codecs also codec module convert byte data type string. Example import codecs byteData b'Lets \xf0\x9f\x8d\x95!' codecs.decode(byteData, 'UTF-8') Output: Lets 🍕 Using str() easiest method convert bytes string. Let's understand following example. Example byteData b'Lets \xf0\x9f\x8d\x95!' str(byteData, 'UTF-8') print(b1) Output: Lets 🍕&! necessary pass encoding argument str() otherwise, will some unexpected results. Wrong encoding leads incorrect output. example pass str() method with UTF-16, will following error. str(b, 'UTF-16') Output: '敌❴\u2073牧扡愠\uf020趟↕' Conclusion This tutorial will help work with byte data type. Thinks like charsets, encoding, binary used encode decode. have defined various methods convert byte data type string. Next TopicPython Program Find Anagram prev next