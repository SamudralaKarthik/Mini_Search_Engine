next prev Rename Column Names Python? Let's modify Pandas DataFrame's column titles. this tutorial, we'll through different methods changing column names pandas DataFrame. Records kept Pandas DataFrame, rectangular matrix. dataFrame makes easy visualize work with data. both rows columns. Each column contains array data particular characteristic attribute, each indicates observation singular event. Every Pandas Dataframe column holds homogenous records throughout particular column. However, Dataframe rows still have heterogeneous homogeneous record datatypes throughout specific row. Unlike two-dimensional Python array, Dataframe pandas marked axes. Ways Renaming Columns Python Using rename() Function Applying rename() method technique rename columns Dataframe. When want rename specific columns, this approach comes handy because need supply data columns that need changed. Example Rename single column Code Python program change name column using rename() function Importing pandas package import pandas Defining dictionary containing data Data frame dictt {'1': ['a', 'b', 'c', 'd', 'e'], '2': ['a1', 'b1', 'c1', 'd1', 'e1'], '3': ['a2', 'b2', 'c2', 'd2', 'e2']} Converting dictionary into Pandas DataFrame df_pd pd.DataFrame(dictt) Before changing name column print("Original dataframe: \n", df_pd.columns) df_pd.rename(columns {'2':'Changed'}, inplace True) After changing name column print("\nAfter changing name:\n", df_pd.columns) Output: Original dataframe: Index(['1', '2', '3'], dtype='object') After changing name: Index(['1', 'Changed', '3'], dtype='object') Example Rename multiple columns Code Python program change name column using rename() function Importing pandas package import pandas Defining dictionary containing data Data frame dictt {'1': ['a', 'b', 'c', 'd', 'e'], '2': ['a1', 'b1', 'c1', 'd1', 'e1'], '3': ['a2', 'b2', 'c2', 'd2', 'e2']} Converting dictionary into Pandas DataFrame df_pd pd.DataFrame(dictt) Before changing name column print("Original dataframe: \n", df_pd.columns) df_pd.rename(columns {'1': 'Changed_1', '2':'Changed_2'}, inplace True) After changing name column print("\nAfter changing name:\n", df_pd.columns) Output: Original dataframe: Index(['1', '2', '3'], dtype='object') After changing name: Index(['Changed_1', 'Changed_2', '3'], dtype='object') Assigning List Different Column Names column names also changed directly changing titles columns defining list with alternative names dataframe column attributes. drawback this approach that, although only wish rename some specific columns dataframe, must offer identifiers them. Code Python program change name column giving list column names Importing pandas package import pandas Defining dictionary containing data Data frame dictt {'1': ['a', 'b', 'c', 'd', 'e'], '2': ['a1', 'b1', 'c1', 'd1', 'e1'], '3': ['a2', 'b2', 'c2', 'd2', 'e2']} Converting dictionary into Pandas DataFrame df_pd pd.DataFrame(dictt) Before changing name column print("Original dataframe: \n", df_pd.columns) df_pd.columns ['Changed_1', 'Changed_2', 'Changed_3'] After changing name column print("\nAfter changing name:\n", df_pd.columns) Output: Original dataframe: Index(['1', '2', '3'], dtype='object') After changing name: Index(['Changed_1', 'Changed_2', 'Changed_3'], dtype='object') Rename Column Names using DataFrame.set_axis() Function this instance, we'll change column's name employing set_axis method. argument, we'll provide updated column name axis that needs change name column. Code Python program change name column using set_axis() function Importing pandas package import pandas Defining dictionary containing data Dataframe dictt {'1': ['a', 'b', 'c', 'd', 'e'], '2': ['a1', 'b1', 'c1', 'd1', 'e1'], '3': ['a2', 'b2', 'c2', 'd2', 'e2']} Converting dictionary into Pandas DataFrame df_pd pd.DataFrame(dictt) Before changing name column print("Original dataframe: \n", df_pd.columns) df_pd.set_axis(['Changed_1', 'Changed_2', 'Changed_3'], axis 'columns', inplace True) After changing name column print("\nAfter changing name:\n", df_pd.columns) Output: Original dataframe: Index(['1', '2', '3'], dtype='object') After changing name: Index(['Changed_1', 'Changed_2', 'Changed_3'], dtype='object') Rename Column Names Using DataFrame.add_prefix() DataFrame.add_suffix() Functions using Suffix Prefix functions, will change column name this instance. will provide prefix suffix that need attached column's initial final names. Code Python program change name column using add_axis() add_prefix() function Importing pandas package import pandas Defining dictionary containing data Dataframe dictt {'1': ['a', 'b', 'c', 'd', 'e'], '2': ['a1', 'b1', 'c1', 'd1', 'e1'], '3': ['a2', 'b2', 'c2', 'd2', 'e2']} Converting dictionary into Pandas DataFrame df_pd pd.DataFrame(dictt) Before changing name column print("Original dataframe: \n", df_pd.columns) df_pd df_pd.add_prefix('s') df_pd df_pd.add_suffix('_p') After changing name column print("\nAfter changing name:\n", df_pd.columns) Output: Original dataframe: Index(['1', '2', '3'], dtype='object') After changing name: Index(['s1_p', 's2_p', 's3_p'], dtype='object') Replace Specific Texts Column Names Using Dataframe.columns.str.replace Function this example, we'll replace method change column's name. argument column, we'll pass original names. Code Python program change name column using add_axis() add_prefix() function Importing pandas package import pandas Defining dictionary containing data Dataframe dictt {'1': ['a', 'b', 'c', 'd', 'e'], '2': ['a1', 'b1', 'c1', 'd1', 'e1'], '3': ['a2', 'b2', 'c2', 'd2', 'e2']} Converting dictionary into Pandas DataFrame df_pd pd.DataFrame(dictt) Before changing name column print("Original dataframe: \n", df_pd.columns) df_pd.columns df_pd.columns.str.replace('1', 'Col_1') df_pd.columns df_pd.columns.str.replace('2', 'Col_2') df_pd.columns df_pd.columns.str.replace('3', 'Col_3') After changing name column print("\nAfter changing name:\n", df_pd.columns) Output: Original dataframe: Index(['1', '2', '3'], dtype='object') After changing name: Index(['Col_1', 'Col_2', 'Col_3'], dtype='object') Next TopicLooping Through DataFrame Python prev next